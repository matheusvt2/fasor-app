---
epic: 5
date: 2026-09-24
verdict: accepted-with-open-items
criteria: declared
headless: true
---

# Epic 5 retrospective: Fill the equipment sheet offline

## Epic summary

- Epic 5 covers stories 5.1 to 5.9. All nine are `done` in `sprint-status.yaml`; `detect-epic --epic 5` returned `story_count: 9`, `pending_stories: []`.
- Diff range: `d1d6aba^..1c94b10`, 13 commits, no merge commits (every PR was squash-merged). `git_evidence.py` ranks the largest additions as `e2e/ficha.spec.ts` (+1290), `packages/domain/src/relatorio/readings.ts` (+626), `apps/web/src/surfaces/ficha/ficha-surface.tsx` (+574), `ficha-fields.tsx` (+360), `readings.test.ts` (+325) and `conclusion.ts` (+307).

  | PR | Scope | Merge commit |
  |---|---|---|
  | #29 | Epic 4 carry-over E4-A1..A4 and the E4-A9 `SyncState` factory (batch F) | bc20c98 |
  | #30 | 5.1 + 5.2 + 5.3 + 5.4 and E3-A3 (batch A) | 44fe33c |
  | #31 | 5.9 and two batch A leftovers (batch C) | 6236a54 |
  | #32 | 5.5 + 5.6 + 5.7 + 5.8 and the nameplate number bug (batch B) | 1e79a3e |
  | #33 | integrated review fixes E5-Q1..Q18 and re-check fixes E5-R1..R3 (batch Q) | ab5a4c2 |

- Coordinator commits on main: `d1d6aba` (epic context), `06316d8`, `b6511e3`, `0f106c2`, `62d9b1c` (status flips), `253215e` and `5e680f2` (integrated review and re-check), `1c94b10` (dated narrowings in `epics.md` and the coordinator notes).
- Evidence used:
  - the specs `spec-epic-4-fix-carry-over.md`, `spec-5-1-5-4-*`, `spec-5-9-*`, `spec-5-5-5-8-*`, `spec-epic-5-fix-qa.md`;
  - the PR bodies of #29 to #33 (narrowings, open questions, known-open findings, verify tails);
  - the integrated review `reviews/epic-5-review-qa.md` (findings E5-Q1..Q18, per-AC results, the re-check after #33 with E5-R1..R3) and its screenshots in `reviews/qa-epic-5/`;
  - the coordinator's notes `reviews/epic-5-coordinator-notes.md` (delivery shape, token figures, W-1..W-7, B-1..B-8);
  - a light aggregate pass by the coordinator over `d1d6aba^..1c94b10` (file sizes, op builders, web-side derivations).
- Missing evidence and narrowed scope:
  - The batch orchestrators' session logs were not read; process lessons come from the coordinator's run, the orchestrators' final reports and the artifacts.
  - `bmad-review` was not invoked on the whole diff, by the invocation's token-economy instruction: the integrated review had already covered the code with emphasis on the cross-batch seams. The retro's own pass was narrowed to size growth, op-builder duplication and web-side derivations, run inline, with no subagent.
  - The retro ran no browser. The E5-R1..R3 fixes (the last commit of #33) were verified by the fix agent's tests only, not by the independent QA agent.

## Findings

### Fixed during the epic

| ID | Finding | Source | Disposition |
|---|---|---|---|
| E5-Q1 (high) | A write refused by `assertSeedPath` (added by E3-A3 in #30) threw a plain `Error`; the server answered 500 and the client retried forever, so the device's sync stuck. Found only because the integrated pass ran `test:e2e:full` (4.2-E2E-002). | `reviews/epic-5-review-qa.md` E5-Q1; fix `SeedPathError` mapped to `op_invalid` in #33 | fixed, re-checked |
| E5-Q2 + E5-R2 (medium) | The insulation table overflowed at 390 px; the first fix left a 58 px input that clipped "250" as "25". | review E5-Q2, re-check E5-R2 | fixed in #33 (input 85 px, e2e asserts `scrollWidth`); R2 not re-checked by QA |
| E5-Q4 (medium) | The composed conclusion claimed readings within criteria and all items conforme when nothing was recorded. | review E5-Q4; `conclusion.ts` | fixed with placeholder wording (OQ-3 open) |
| E5-Q5, Q7-Q10, Q14-Q17, Q18 a/c/d, R1, R3 | Stale criteria line, "147 mΩ" read as MΩ, a keyboard race on the tri-state, the expired-calibration color, `aria-disabled` instead of `aria-readonly`, ungrouped read-only numbers, outlier factor flooring, two block ops bypassing `putBlockOp`, no "Limpar conclusão", e2e gaps, a test reading the outbox too early, an empty "Critérios usados" heading. | review table and re-check | fixed in #33 |
| PR #30 known open | Nameplate number reformatted mid-typing (HIGH), Repetir overwrote answered rows, the cabine tree row. | PR #30 body; review "Known-open" | fixed in #32 and #31, refuted by QA |
| Batch reviews | A: `parseDecimalPtBr` leading-zero bug, an NA default that could not be committed, a stale Desfazer wiping a correction. B: 13 of 21 findings. C: 2 of 5. | PR bodies #30-#32 | fixed before each merge |

### Open: carried from the review (not fixed by design)

| # | Lens | Finding | Source | Disposition | Upstream lesson |
|---|---|---|---|---|---|
| E5-Q3 | seam (AR-17) | A concluded sheet edited back to incomplete still counts as Concluída; there is no "concluída com pendências" flag. | `sheet-state.ts:71`; review E5-Q3 | defer to Epic 7 (OQ-2) | none |
| E5-Q6 | safety (product) | "120.001" in a ratio or contact cell reads 120001 by the AC's thousands rule; the echo cannot tell the readings apart. | `pt-br-number.ts`; review E5-Q6 | product question (OQ-1) | A parse rule that can change a signed value gets a per-table-kind decision in the spec. |
| E5-Q11-Q13 | product | Step collapse with an amber reading; "Igual à" copying Nº SÉRIE; the not-tested reason preselected with no history. | review E5-Q11..Q13 | product questions (OQ-4..OQ-6) | none |
| E5-Q18 b/f/g | test gap | "Copiar da última visita", the "Salvo" throttle and the remaining listed paths have no e2e. | review E5-Q18 | defer ("Copiar da última visita" with Epic 7) | none |
| #32 known open | edge case | A stored `criterion_override` is ignored; clearing the result after confirmation still leaves the text printable; confirming while the block changes can store a stale basis. | `readings.ts:240`; `conclusion.ts:263`; review "Known-open" | fix with Epic 7's print path | none |
| #29 known open | edge case | Offline readiness trusts a held relatório whose first download was cut; the "unchanged from Em campo" path is covered only in jsdom. | review "Known-open" | accept (trade-off recorded in #29) | none |

### Open: aggregate views (retro pass)

| # | View | Finding | Source | Disposition |
|---|---|---|---|---|
| G-1 | size growth | `setup-surface.tsx` grew from 845 to 895 lines in #29, although E4-A7 said to split it "when next touched". | `wc -l apps/web/src/surfaces/relatorio/setup-surface.tsx`; E4-A7 | defer (E4-A7 stays open) |
| G-2 | size growth | `readings.ts` (622 lines) and `ficha-surface.tsx` (541) are the new largest files; `readings.ts` holds parsing, evaluation, run targets and outliers together. | `wc -l` at `1c94b10` | accept for now; split when Epic 8 wires suggestions into readings |
| G-3 | duplication / ownership | Op paths are still composed as strings in `apps/web`: `ficha-ops.ts:16-35` (sheet, location se/env) and `relatorio-ops.ts:39-61` (location, equipment), and `setup-surface.tsx:130,258` hand-builds `relatorio/setup/*` puts outside any builder. E4-A4 unified only status and block puts. | files and lines cited | fix when next touched (fold into E4-A7): kernel path builders for every family the web writes |
| G-4 | ownership | No web-side status, count or plural derivation was found in `apps/web/src/surfaces/ficha` (grep for filters with `.length`, NC/C comparisons and plural choices returned nothing); the review found one reading evaluation shared by the UI, `sheetProgress`, `suggestConclusionPair` and `composeConclusion`. | grep at `1c94b10`; review | none (clean) |

### Test gate

- Every story has at least one `@p0` e2e; `verify` grew from 51 to 70 `@p0` tests and `test:e2e:full` from 90 to 123 (PR #29 and #33 tails).
- `test:e2e:matrix` passed at epic close (23 of 30, 7 skipped), but no Epic 5 test runs on Android Chrome or WebKit, although the sheet is the tablet's main surface. The ACs that depend on a real keyboard (5.1 AC2 "the bar sits above the keyboard", 5.5 AC2 the M · G · T chip row above the phone keypad) have only emulated evidence.
- Timing flakes hit almost every full run: 3.6-E2E-001 (E3-A4, open since Epic 3), and once each 4.1-E2E-002, 4.3-E2E-002, 1.6-E2E-001, 5.5-E2E-001 and the E5-Q8 e2e. Batch C needed seven verify attempts, the last after `docker compose down -v`. Every one passed alone (PR #29, #31, #32, #33 bodies; coordinator notes B-3).

### Process

| ID | Finding | Source | Lesson |
|---|---|---|---|
| P-1 | Token economy worked. Orchestrators reported 269k-498k tokens against 450-600k in Epic 4, and the integrated QA 363k over two rounds against about 820k. The shape: one instruction file, the epic context compiled once, two internal review layers, no per-batch independent review or browser pass, one integrated review on clean main, resumes via SendMessage. | `epic-5-coordinator-notes.md` table; Epic 4 retro P-7 | Keep this shape while tokens are scarce. |
| P-2 | The integrated pass still carried the quality: it found the only high defect (E5-Q1) and the 390 px clipping, and its re-check found E5-R2 in the Q2 fix. | review; coordinator notes W-5 | The integrated pass is not optional when per-batch reviews are cut. |
| P-3 | Re-cutting wave 2 by data dependency (readings and the conclusion that judges them in one batch) avoided a second "out of criterion" rule. | coordinator notes W-2; G-4 | Cut batches along data dependencies, not story numbers. |
| P-4 | A sonnet orchestrator backgrounded its review subagents and was handed back with nothing committed, the fifth such stall across Epics 3-5. The sonnet orchestrators also reported more tokens (463k, 498k) than the opus ones (269k, 355k). | coordinator notes B-1 and table | Orchestrate on opus; keep the "both layers in one message, blocking" and "WIP commit before review" lines. |
| P-5 | A kernel validation that refuses a write shipped without being mapped into the sync refusal contract (E5-Q1). | review E5-Q1 | Any new kernel refusal gets an api integration test through the sync route. |
| P-6 | Narrow-width defects surface only at the integrated pass when batches skip the browser pass (E5-Q2, then E5-R2). | coordinator notes B-4 | A batch that adds a table or a wide control asserts its fit at 390 px in an e2e (cheap, no MCP pass). |
| P-7 | About fifteen product questions are open, and the conclusion recommendations and empty-sheet wording are authored placeholders. The human gates E1-A1, E3-A1, E3-A2 and E4-A6 are still open while Epic 5 shipped on seed v1. | review "Open questions"; coordinator notes B-5, B-6 | Batch the questions into one session with Matheus and Bruno before Epic 7 prints the sheet. |

## Behavior verification

The integrated QA agent exercised Epic 5 end to end on an isolated stack (`fasor-qa5`) at `62d9b1c`, and again at `a5b3559` (PR #33 before its last commit), with the Playwright MCP browser at 1280, 768 and 390 px, light and dark, a 470 px short viewport, keyboard-only on the tri-state and Conclusion radiogroups, offline with "Sincronizar agora", and a dead-tab draft:
- a relatório created through the UI from the standard template; sheets CE-ENEL, PR-ENEL, PR-ENEL-2, TP-ENEL and SEC-ENEL;
- cabine data, the copy chips, nameplate by kind, checklist taps, NC chips, bulk Conforme with undo, Repetir;
- readings with "3.300", "0.330", "147G", the unit toggle, amber out-of-criterion, the outlier helper, an Enter-only run of nine readings ending on "Próxima ficha", Não medido, the ratio calculation;
- the instrument picker with an expired instrument and with none registered (offline "Cadastrar instrumento");
- the suggestion, Confirmar, Editar and Substituir, "Concluir ficha" to the next sheet, Não ensaiado with Desfazer before and after sync;
- the Epic 4 carry-over: generate, edit, Em revisão, generate revision 2, and the two-device reuse.

Per-AC results are in `reviews/epic-5-review-qa.md`: every AC passes after #33 except 5.3 AC4 ("Copiar da última visita", partial: kernel tests only, unreachable before Epic 7). The final PR #33 head (`479aab1`) passed `pnpm verify` (unit 809 + 752 + 20, api 144, e2e `@p0` 70) in the fix agent's run; its E5-R1..R3 changes were not re-checked in a browser by the QA agent. The retro itself ran no browser.

## Previous-retro follow-through

Headless run: transitions are proposed only; no status was written.

- `epic-1-retro-item-1-a1-run-the-manual-ipad-and-android-offli`: no evidence found (`test-artifacts/manual/` has no new run). Proposed: none. It was "blocking before Epics 5 and 6"; Epic 5 shipped without it.
- `epic-1-retro-item-12-a12-before-any-exposure-beyond-the-offic`: not applicable yet. Proposed: none.
- `epic-2-retro-item-16-e2-a4-dod-every-user-facing-ac-exercised`: every Epic 5 story has a `@p0` and the narrowings carry dates (`1c94b10`), but 5.3 AC4 stays kernel-only. Proposed: stays `in-progress`.
- `epic-3-retro-item-18-e3-a1-record-the-r-009-seed-review-under`: no evidence; Epic 5 ran on seed v1 (NA defaults, subtypes and field keys now drive the sheet). Proposed: none.
- `epic-3-retro-item-19-e3-a2-decide-the-not_tested_reason-for-t`: no evidence; #31 left the fixture unchanged on purpose. Proposed: none.
- `epic-3-retro-item-21-e3-a4-stabilize-the-gate-templates-list`: not landed; 3.6-E2E-001 failed in most full runs of this epic. Proposed: none.
- `epic-3-retro-item-22-e3-a5-decide-d-4-template-version-d-5-co`: D-5 (contact-insulation row offset) is partly addressed by the cell addressing of #32 (`row` counts across a test's tables; `packages/domain/src/ops/path.ts` header); D-7 is still open. Proposed: stays `in-progress`.
- `epic-3-retro-item-23-e3-a6-process-a-seed-fixture-freeze-requ`: no seed freeze happened; #30 regenerated the `replay-small` golden (a synthetic test fixture, not the Porto Seguro seed). Proposed: none.
- `epic-3-retro-item-26-e3-a9-deferred-polish-toggle-sub-lines-f`: no evidence. Proposed: none.
- `epic-3-retro-item-27-e3-a10-tooling-remove-stale-worktrees-re`: `git worktree list` shows 26 entries. Proposed: stays `in-progress`.
- `epic-4-retro-item-32-e4-a5-spec-reconciliation-dated-narrowin`: the narrowings for items 27, 28, 30 and 31 are not in `epics.md` (grep); item 25 is undecided. Proposed: none.
- `epic-4-retro-item-33-e4-a6-bruno-reads-the-skeleton-docx-revi`: no evidence. Proposed: none.
- `epic-4-retro-item-34-e4-a7-refactor-when-next-touched-split-s`: not landed; `setup-surface.tsx` grew (G-1). Proposed: none.
- `epic-4-retro-item-35-e4-a8-process-integrated-human-style-qa`: landed. An integrated human-style QA ran on clean main before close (`253215e`, re-check `5e680f2`); every batch prompt named its cross-batch contracts and owners; E4-A2 got a two-device test (E4-E2E-002); product choices went to open questions in every PR body. Proposed: `done`.
- `epic-4-retro-item-36-e4-a9-gate-run-test-e2e-matrix-at-epic-c`: partly landed. `test:e2e:matrix` ran at close (review "What ran"), `makeSyncState()` exists (PR #29), and at most two batches (two verifies) ran at a time; no orchestrator watchdog was built. Proposed: `in-progress`.

## Action items

All items are proposals. Code items go through the normal dev loop; product and spec items wait for Matheus.

1. **E5-A1 (fix before Epic 6, gate)**: Stabilize the e2e gate as a real story: fix 3.6-E2E-001 (E3-A4) and the timing flakes seen this epic (4.1-E2E-002, 4.3-E2E-002, 1.6-E2E-001, 5.5-E2E-001, the E5-Q8 test) by waiting on committed state instead of fixed timeouts, and make a full run green three times in a row. Owner: dev (opus).
2. **E5-A2 (test, touch)**: Add the sheet to the durability matrix: tri-state taps, the phone unit chip row, the sticky bar with the on-screen keyboard, Não ensaiado; and include the sheet in the E1-A1 manual device script. Owner: dev, then Matheus for the device run.
3. **E5-A3 (product decisions)**: One session with Matheus (and Bruno for wording) on OQ-1..OQ-6 of `reviews/epic-5-review-qa.md`, the carried questions (80 % humidity threshold, CONDIÇÕES when out, the single TAP row, the fixture's kV unit, Q15, item 23), and the authored conclusion recommendations and empty-sheet wording; record each answer as a dated note under its story. Owner: Matheus.
4. **E5-A4 (small fixes, with Epic 7's print path)**: apply `criterion_override` (`readings.ts:240`); a cleared result must not leave a printable confirmed text (`conclusion.ts:263`); guard the stale basis on confirm; the E5-Q18 remaining e2e. Owner: dev.
5. **E5-A5 (refactor when next touched)**: kernel path builders for every op family the web writes (G-3), folded into E4-A7 with the `setup-surface.tsx` split (G-1). Owner: dev.
6. **E5-A6 (process, DoD)**: any new kernel refusal of a write ships with an api integration test through the sync route (P-5); a batch that adds a table or wide control asserts its 390 px fit in an e2e (P-6). Owner: coordinator / SM (add both lines to the batch instruction file).
7. **E5-A7 (process, token economy)**: keep the Epic 5 delivery shape while tokens are scarce (P-1, P-2), orchestrate on opus (P-4), and cut batches along data dependencies (P-3). Owner: coordinator.

## Acceptance verdict

**Accepted with open items**, against the declared criteria (the Epic 5 ACs in `epics.md`, read with their dated narrowing notes of `1c94b10`).

- Every story is `done`; `pending_stories` is empty.
- The integrated review and its re-check show every AC passing on main after #33, with one partial (5.3 AC4, unreachable before Epic 7) and the only high defect (E5-Q1) fixed and re-checked.
- Open items: the product questions (E5-A3), the touch and device evidence for keyboard-dependent ACs (E5-A2, E1-A1), the unstable gate (E5-A1) and the small known-open edge cases (E5-A4). None corrupts a sheet in the tested flows.
- By process lesson (d) of `bmad-build.toml`, `epic-5` stays `in-progress` in `sprint-status.yaml` until the device evidence (E1-A1 with the sheet, E5-A2) exists.

## Open questions

1. OQ-1..OQ-6 of `reviews/epic-5-review-qa.md` (thousands dot in ratio and contact cells; edits after "Concluir ficha"; the empty-sheet conclusion wording; step collapse with an amber reading; "Igual à" and per-unit fields; the default not-tested reason).
2. Does Epic 6 start before the E1-A1 device proof, which was declared blocking for Epics 5 and 6?
3. Does the R-009 seed review (E3-A1) still gate anything, now that the sheet runs on seed v1's NA defaults and field keys?

## Assumptions

- Epic 5 was taken from the invocation (`-H`, "Epic 5"); `detect-epic --epic 5` returned `story_count: 9` and `pending_stories: []`.
- The machine verdict `accepted-with-open-items` was rendered on the evidence alone, with no human decision.
- The invocation limited the diff review to cross-story seams not in the QA review; `bmad-review` was not run on the whole diff, and the retro's aggregate pass ran inline.
- Every action item above is proposed, not applied; no previous action item's status was changed by this retro.
- `epic-5` is left `in-progress` by lesson (d); only the `epic-5-retrospective` key is set to `done`.
