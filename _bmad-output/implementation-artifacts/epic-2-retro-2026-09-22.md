---
epic: 2
date: 2026-09-22
verdict: accepted-with-open-items
criteria: declared
headless: true
---

# Epic 2 retrospective: Registries and company identity

## Epic summary

- Epic 2, stories 2.1 to 2.6. All six are `done` in `sprint-status.yaml`; `detect-epic --epic 2` returned `pending_stories: []`.
- Diff range `7bb4fbf..ad0f353`: 90 files, +6091/-82 in `apps`, `packages`, `e2e` and `scripts`.
- The work shipped as three PRs:

  | PR | Stories | Merge commit |
  |---|---|---|
  | #13 | 2.1 | bca7460 |
  | #14 | 2.4 + 2.5 + 2.6 | 7df54a9 |
  | #15 | 2.2 + 2.3 | 4d6eb18 |

  PR #16 (2a27287) was a follow-up fix to the phone tab selector.
- Evidence used:
  - `reviews/epic-2-review-qa.md`: an independent code review of the whole diff, plus a human-style Playwright pass on an isolated stack at 768 and 390 px, online and offline, with screenshots in `reviews/qa-epic-2/`.
  - The specs `spec-2-1-*.md`, `spec-2-2-2-3-*.md`, `spec-2-4-2-5-2-6-*.md` and `spec-registries-phone-tab-selector.md`.
  - `deferred-work.md` and the Epic 1 retro.
- Missing evidence: the build sessions' logs were not read, so process lessons come from the artifacts only. `bmad-review` was not invoked separately. The single independent code-review subagent stands in for it, so that part of the review has a narrower scope.

## Findings

Every finding below was re-checked against its primary source during this run.

| ID | Lens | Finding | Source | Disposition | Upstream lesson |
|---|---|---|---|---|---|
| D-1 | cross-story / edge case | The server merge of manufacturer and voltage-class names leaves a ghost row on the device that sent the ops. Later edits to that id are acked and lost without an error. | `apps/api/src/sync/apply.ts:206-248` (the redirect lives only in the per-request `idRedirects`). Reproduced in the browser; the server row showed the lost gender. | fix now | A server rewrite of a client op must send a system op back, and a two-cycle convergence test proves it. |
| D-2 | spec reconciliation | `RegistryPickerField` is mounted on no screen. AC 2.4-2, 2.5-2 and 2.5-3 are covered only by `components/registry-picker-field.test.tsx`. The instrument's Fabricante field is free text. | `grep RegistryPickerField apps/web/src/surfaces` finds only a comment in `word-registry-tab.tsx:58` | fix now: wire it into the instrument form. AC 2.4-2 moves to Epic 4 (decision by Matheus). | A user-facing AC closes only through a mounted screen. |
| D-3 | AC gap | The certificate cannot be opened from the UI. `fetchFile` is used only by the Empresa preview. | `apps/web/src/surfaces/registries/empresa-tab.tsx:241`; the certificate tile offers only "Substituir" | fix now | same as D-2 |
| D-4 | verification gap | Story 2.3 has no `@p0` e2e test, so `pnpm verify` never runs the Empresa tab. | `e2e/cadastros.spec.ts:551,603,636` (`@p1`, `@p2`, `@p1`) | fix now | Merge checklist: at least one `@p0` per story. |
| D-5 | UX / AC 2.3 | The form defaults are invisible until the first field is committed, while the preview already prints them. | screenshots `qa-epic-2/02` vs `03` | fix now | none |
| D-6 | AC 2.5 | The voltage-class form reuses the manufacturer form. It shows "Nome" and "Gênero/Número gramatical", accepts "abc" and has no kV. | browser pass; `word-registry-tab.tsx` shares one panel for both kinds | fix now | Check a form against the AC wording, not only against its sibling. |
| D-7 | AC 2.1 | On the phone the instrument form is inline, not full-screen, and the row body and meta run together. | screenshot `qa-epic-2/05` | fix now | Check the phone layout of every form. |
| D-8 | polish | Duplicate empty-state CTAs with no sentence; the Código is read-only after creation; "Descartar" names the dismiss buttons; the Home Cadastros subtitle is incomplete; `device_id` is stamped twice in `db/commit.ts:192-194`; a second blur re-emits an unchanged value; the Empresa CNPJ is not validated; the "Sem logo" note stays when a logo exists. | `reviews/epic-2-review-qa.md` D-8 and the 2.3 rows | fix now (cheap) | none |
| W-1 | debt | Two `PreIssueRow` shapes came out of the batched PRs. | `packages/domain/src/checks/pre-issue.ts`, `pre-issue-client.ts`; already in `deferred-work.md` | defer to Epic 7 | Batching parallel stories needs a reconcile step before merge. |

What went well (sourced in `reviews/epic-2-review-qa.md`):
- The upload pipeline is rigorous: idempotency, `409 file_row_missing`, the size and hash checks, and failure isolation.
- Ownership discipline held.
- `compareCriterion` refuses incompatible units.
- Offline create works, and the push runs about 1 s after reconnecting.
- The `deferred-work.md` entries are precise.

## Behavior verification

The review exercised these flows end to end on the `fasor-qa2` stack at `main` ad0f353: instruments (create, expired sort, remove with undo, offline create), certificate upload (size and type refusals, upload to "Enviado", PUT idempotency), Empresa (fields, logo and cover variants, live preview), Clientes (CNPJ validation, sites), Fabricantes and Classes (server merge), Critérios (read-only table at 390). The per-AC results are in the review document.

## Previous-retro follow-through

Epic 1 retro items that are not `done` in `sprint-status.yaml`:
- `epic-1-retro-item-1-a1-run-the-manual-ipad-and-android-offli`: no evidence found that the manual device run happened. Proposed status: none (stays `open`, it is Matheus's to run).
- `epic-1-retro-item-12-a12-before-any-exposure-beyond-the-offic`: not applicable yet; nothing has been exposed beyond the LAN. Proposed status: none.

Epic 1 lessons that did carry into Epic 2: every PR had a real-browser pass with screenshots (`reviews/story-2-1`, `2-2-2-3`, `2-4-2-5-2-6-registries-batch`), and each `.frame-*` translation lives in `app.css`. Headless run, so no status was written.

## Action items

All of these are proposed remediation for the normal dev loop.

1. **E2-A1**: Fix D-1. The server emits a system op that retires the merged-away id on the originating device and persists the redirect. Add a two-cycle convergence integration test and an e2e test. Owner: dev (fix PR).
2. **E2-A2**: Fix D-2. Wire `RegistryPickerField` into the instrument form's Fabricante field, with an e2e test that creates "Celtta" offline. Strike AC 2.4-2 through in `epics.md` and move it to the Epic 4 Novo relatório story, with the date. Owner: dev (fix PR). The spec reconciliation was decided by Matheus.
3. **E2-A3**: Fix D-3 to D-8, with an e2e test for each user-visible change and one `@p0` for Empresa. Owner: dev (fix PR).
4. **E2-A4**: Process. Add to the story DoD: "every user-facing AC is exercised through a mounted screen, or moved in `epics.md` with a date", and "at least one `@p0` per story". Owner: Matheus / SM.
5. **E2-A5**: Process. The human-style pass covers each form at phone 390 and runs one second-cycle or second-device scenario. Owner: dev and reviewers.

## Acceptance verdict

**rejected** (machine, declared criteria). All stories are `done`, but these criteria are not met: AC 2.1 (full-screen form on phone), AC 2.2-3 (open the certificate from the row), AC 2.5-2/3 (chip row and inline create cannot be reached), and AC 2.5-4 (the merge diverges on the originating device, D-1, which is blocking because it loses data without an error). The verdict is expected to move to accepted-with-open-items once E2-A1 to E2-A3 merge. Only W-1 would then stay deferred.

### Update 2026-09-22 (after PR #17)

~~**rejected**~~ changed to **accepted-with-open-items**, based on PR #17 (squash `e0c9fee`):
- E2-A1 to E2-A3 fixed D-1 to D-8. `pnpm verify` was green, with its output pasted in the PR.
- An independent review approved the PR: https://github.com/matheusvt2/fasor-app/pull/17#issuecomment-5786634141

Items that stay open:
- W-1 (deferred to Epic 7).
- The process items E2-A4 and E2-A5.
- The out-of-scope notes in `spec-epic-2-fix-retro-items.md`: a rename collision is not merged, voltage classes sort as text, and on the phone the app bar under the full-screen form can still be reached by keyboard.
- The review's low note that `sameFieldValue` lives in `apps/web`.

## Open questions

- Should "Remover" on a registry row stay a soft remove (`removed_at`)? The code review noted it is identical to "Arquivar" apart from the toast wording.

## Assumptions

- The epic came from the invocation (`-H epic 2`). `detect-epic --epic 2` returned story_count 6 and no pending stories.
- The verdict was rendered with no human acceptance decision. Matheus's decisions cover remediation (fix D-1 to D-8 in one PR, the D-2 split), not acceptance.
- The action items above are proposals appended as `open`.
