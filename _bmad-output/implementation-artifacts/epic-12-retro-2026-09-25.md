---
epic: 12
date: 2026-09-25
verdict: accepted-with-open-items
criteria: declared
headless: true
---

# Epic 12 retrospective: Field journey and visual refresh

## Epic summary

- Epic 12 covers stories 12.1 to 12.6, added by `sprint-change-proposal-2026-09-24.md` from the journey review of 2026-09-24 and run before Epic 6 (confirmed by Matheus the same day). All six are `done` in `sprint-status.yaml`; `detect-epic --epic 12` returned `story_count: 6`, `pending_stories: []`.
- Diff range: `144a558..130eaa2`, 174 files, +8165 / -1017, no merge commits (every PR squash-merged). The largest additions are e2e specs (`sheet-knows-12-3-12-4.spec.ts` +601, `journey-forward.spec.ts` +355, `lost-taps.durability.spec.ts` +324, `journey-taps.spec.ts` +251, `tap-budget.spec.ts` +237) and the kernel (`screen-label.test.ts` +230, `sheet-progress.ts` +172/-17). The api changed in 3 files (+228), all for the E12-Q4 template upgrade.

  | PR | Scope | Merge commit |
  |---|---|---|
  | #34 | E5-A1 gate stability (another session, merged while wave 1 ran; not Epic 12 scope) | e546e0e |
  | #35 | 12.2 forward navigation (batch B) | 87c6579 |
  | #36 | 12.1 lost taps and section collapse (batch A) | b63acbe |
  | #37 | 12.3 + 12.4 sheet, cabine, instrument, plate, NC (batch C) | 8b4777b |
  | #38 | 12.5 + 12.6 v0.9 visual direction and the tap budget (batch D) | 4f769e0 |
  | #39 | integrated review fixes E12-Q1..Q14 (batch Q) | eb298d2 |

- Coordinator commits on main: `144a558` (review, DESIGN.md v0.9, Epic 12, epic context), `a88c917`, `f027678`, `66fe410`, `48e3daf` (status flips), `c0ce7b0` (integrated review, J3 narrowing in `epics.md`), `22f2d29` (re-check, E12-R1/R2 deferred), `130eaa2` (coordinator notes). `f19df65` (Epic 6 context) is outside Epic 12 scope.
- Evidence used: `reviews/epic-12-coordinator-notes.md` (delivery shape, tokens, W-1..W-6, B-1..B-8), `reviews/epic-12-review-qa.md` (E12-Q1..Q14, per-AC table, tap counts, re-check with E12-R1/R2), `epic-12-context.md`, the Epic 12 lines of `epics.md` that the review cites (grep only), `sprint-change-proposal-2026-09-24.md`, `git log 144a558^..HEAD`, `git diff --numstat`, `deferred-work.md` (grep), `sprint-status.yaml`.
- Missing evidence and narrowed scope:
  - The orchestrators' session logs and the PR bodies were not read (token economy); process lessons come from the coordinator notes and the review.
  - `bmad-review` was not invoked on the diff, by the invocation's token-economy instruction; the integrated review had covered the product diff story by story with cross-batch lenses. The retro's own pass was a light aggregate check (file growth, one ledger check, one DoD check), inline, no subagent.
  - The retro ran no browser and no test. The re-check after #39 covered seven items (Q1, Q2, Q4, Q6, Q7, Q11, Q13) in the browser or by reading; the fixes for Q3, Q8, Q9, Q10, Q12 and Q14 rest on the fix agent's tests and PR #39's green `verify`, not on an independent re-check.

## Findings

### Fixed during the epic

| ID | Finding | Source | Disposition |
|---|---|---|---|
| J-01 (review) | Four taps lost per session: a focus-driven collapse moved the target and a commit re-render landed between pointer up and a delayed click. | `epic-12-context.md`; coordinator notes "12.1 root cause"; `apps/web/src/input/press-hold.ts` | fixed once in the shared press path (#36); red then green on desktop, Android emulation and WebKit (matrix EXIT 0) |
| E12-Q1 (high) | "Outro…" then "Criar “15 kV”" wrote `tensao_de_placa "15"` and then `null` on the next blur, while the field still read "15 kV". Pre-existing, reached by the shortened J1 path. | review E12-Q1; `registry-picker-field.tsx:115-118` | fixed in #39 (input takes the created row's label, `voltageMatchKey`); re-checked, no `null` put |
| E12-Q2 | Scroll and focus carried over on every forward navigation (Cadastros opened at `scrollY` 750, next sheet mid-checklist, focus on `BODY`). | review E12-Q2 | fixed in #39; re-checked at 768 and 390 |
| E12-Q3 | Two new `@p0` specs flaked in the gate (stale App bar title, Voltar before the back-target effect). | review E12-Q3; `e2e/journey-forward.spec.ts:83-90` | fixed in #39 (`--repeat-each 5` green); not re-checked by QA |
| E12-Q4 | A seed version bump never reached existing companies: databases seeded before #37 kept the v1 standard template, so D-3 and the third Não ensaiado reason were invisible there. | review E12-Q4; `apps/api/src/db/seed.ts` | fixed in #39: server-only `template/{id}/seed_version`, `CONTRACT_VERSION` 3, upgrade only while `version` = 1, one transaction under `lockCompany`; re-checked by reading |
| E12-Q5 | The J3 target (5 taps / 40 keys) assumed a copied serial that D-3 forbids. | review E12-Q5 | amended with dated strikes in `epics.md:2432` and `:2508` (`c0ce7b0`): J3 7 taps / 47 keys |
| E12-Q6 | The gate stayed green with `useHeldWhilePressed` removed. | review E12-Q6 and re-check | fixed in #39; mutation run: 12.1-E2E-001 fails 2 of 2 without the hold. Residue E12-R2 below |
| E12-Q7, Q11, Q13 | Focus outside the gesture after "Outro…", focus and instrument check after the J5 creates, focus after Não ensaiado "Outro". | review | fixed in #39; re-checked (Q7 in Chrome only) |
| E12-Q8..Q10, Q12, Q14 | Missing-count emphasis (kernel `sheetSummaryParts`), a split "25 °C", "campos da placa" naming cabine fields, a trailing separator in the card name, the rail strip height. | review; coordinator notes batch Q | fixed in #39 by the fix agent's tests; not re-checked by QA |

### Open: carried from the review and the re-check

| # | Lens | Finding | Source | Disposition |
|---|---|---|---|---|
| E12-R1 | edge case (sync) | An offline edit of the v1 template synced after the upgrade writes v1-derived blocks under `seed_version` v2; harmless today because v1 and v2 differ only in seed definitions. The upgrade runs only when the seed CLI runs, not on API start. | review re-check E12-R1; `deferred-work.md:683` | deferred; guard when a seed version changes template block shape (E12-A4) |
| E12-R2 | test | 12.1-E2E-007 alone can pass without the hold (1 of 2); 001 carries the gate. | `deferred-work.md:689` | Epic 6 batch F owns it (carry-over list) |
| D-2 residue | product | A section left by a tap stays open for the rest of the visit. | review "Open questions" 3 | product question (E12-A1) |
| D-4 residue | product | Right after the last value, a primary still labelled "Próxima ficha" concludes the sheet and writes the suggested instruments (`ficha-surface.tsx:311`). | review "Known-open" and "Open questions" 4 | product question (E12-A1) |
| Authored copy | product | "Ficha completa", "falta ⟨campo⟩" / "faltam ⟨n⟩ campos", the pre-issue row text, the "Equipamento inacessível" justification, the printed OBSERVAÇÕES with NC items, the TAG field's final form, status on generate (J-19). | review "Open questions" 5-6; coordinator notes B-8; proposal § 5 | product questions (E12-A1) |
| Device | device risk | Soft keyboard after the programmatic focus of "Outro…" on iPadOS or an Android WebView; the lost-tap fix on a real tablet. | review E12-Q7 (device check still open) | E12-A3 |
| Visual | visual | 1024 landscape never captured (12.5 AC2 names it); stepper double underline at 768 and 1280; section 9 header narrow at 390 not re-captured; a disabled step named in the header, unused `is-done`, two sentence-case rules deferred. | review 12.5 AC2 row and "Known-open"; coordinator notes batch D | E12-A7 |

### Open: aggregate views (retro pass)

| # | View | Finding | Source | Disposition |
|---|---|---|---|---|
| G-1 | ledger drift | The prefilled TAG is never written as an op; Epic 7's printer and the `last_nameplate` projection must read `nameplateTagPrefill`. The coordinator notes and the review say this is tracked in deferred-work, but `deferred-work.md` has no such entry (grep for `nameplateTagPrefill`, `12.3`, "Do bloco"); it lives only in the spec's frontmatter and triage log. | `spec-12-3-12-4-sheet-cabine-instrument-plate-nc.md:19,142`; coordinator notes batch C "Known open"; review "Known-open" row | E12-A2 |
| G-2 | size growth | `setup-surface.tsx` grew again, 895 to 957 lines, although E4-A7 says to split it when next touched; `ficha-surface.tsx` 541 to 578. | `git show 144a558:…` vs `wc -l` at `130eaa2` | E4-A7 stays open; Epic 6 batch F owns it |
| G-3 | DoD check (E5-A6) | The new device-refused family `template/{id}/seed_version` is refused through the shared `isServerOnly` rule, tested in the kernel (`packages/domain/src/ops/path.test.ts:86-87`); the sync-route refusal is tested with an older server-only family (`apps/api/src/sync/sync.integration.test.ts:424`). No family-specific route test, but the mapping is generic. | files cited | accept |
| G-4 | ownership | The new rules (`suggestedInstrument`, `cabineProgress`, `screenLabel`, `sheetSummaryText` / `sheetSummaryParts`, `nameplateTagPrefill`, the suggested observation) are kernel functions; the review's ownership lens found no web-side derivation. | review "What ran" code review row; spec-12-3-12-4 line 91 | none (clean) |

### Test gate

- The tap budget is now a `@p0` test (`e2e/tap-budget.spec.ts`, one constant `TAP_BUDGET`), and ERGO-E2E-001 asserts 48 x 48 / 56 px targets at 390 and 768 with no exclusions: a later story cannot add taps or shrink targets unnoticed.
- The integrated `verify` went red on two new flaky 12.2 specs (E12-Q3); `test:e2e:full` (151 tests) and `test:e2e:matrix` (48 tests) were green, and the matrix now carries the lost-tap race on Android Chrome emulation and WebKit.
- Known flakes that pass alone recurred: 4.1-E2E-002 (batch B), 3.6-E2E-001 (batch D), `home-surface.test.tsx:338` once (batch A). E3-A4 stays open; Epic 6 batch F owns it.

### Process

| ID | Finding | Source | Lesson |
|---|---|---|---|
| P-1 | Measuring before building worked: each batch had a baseline from the review's § 6 scripts and each PR a before/after. J1 copied plate 24 taps + 1 lost / 87 keys to 9 / 36; J0 2 to 1; J2 observation typed twice to 2 taps, 0 keys; J6 about 15 s to about 7 s. | coordinator notes W-1; review "Tap counts" | Keep journey measurements as `@p0` budgets. |
| P-2 | Token economy improved again: 216k-288k per batch (Epic 5: 269k-498k), QA 321k plus a 28k cached re-check; every agent on opus; waves cut by surface with no manual conflict. | coordinator notes table, W-3, W-5 | Keep the shape (E5-A7 applied). |
| P-3 | The integrated pass again found what no batch saw: the only high defect (E12-Q1) and the seed-upgrade gap (E12-Q4). | coordinator notes W-4 | The integrated pass stays mandatory (E4-A8 applied again). |
| P-4 | A review target computed without the same review's own decisions (J3 5 taps vs D-3) reached two stories as an unmeetable AC. | coordinator notes B-1; E12-Q5 | Recompute every numeric target against the decisions of its source before the story is ready (E12-A5). |
| P-5 | A seed bump shipped without an upgrade path for existing companies; the fix needed a new op family and a contract bump. | coordinator notes B-2; E12-Q4 | A seed version bump names how existing data reaches it (E12-A4). |
| P-6 | New specs flaked in the gate by waiting on "non-empty" instead of the committed target state, and a mechanism fix left the gate green without it until a mutation run proved otherwise. | coordinator notes B-3; E12-Q3, Q6 | Wait on the target state; prove a mechanism fix with a mutation run (E12-A6). |
| P-7 | Agent logistics: a second session worked on the same checkout and edited the coordinator's uncommitted `epics.md`; the QA agent could not write outside its worktree; about twenty identical interim notifications reached the coordinator. | coordinator notes B-4, B-5, B-6 | E12-A6. |
| P-8 | Promoting the v0.9 CSS repainted the "before" mocks; the before survives only as screenshots. | coordinator notes B-7 | E12-A7. |
| P-9 | Decisions were taken and dated where the documents allowed (J3 threshold, upgrade only while unedited), but product questions keep accumulating for Matheus and Bruno. | coordinator notes W-6, B-8 | Fold into the E5-A3 session (E12-A1). |

## Behavior verification

The integrated QA agent exercised Epic 12 end to end on an isolated stack (`fasor-qa12`) at `48e3daf`, through the UI with the Playwright MCP browser: a relatório created from nothing on seed v2, journeys J0, J1, J2, J3, J4, J5 and J6 counted by a page-level listener, widths 390, 768 and 1280 in light and dark, a 768 x 520 viewport, keyboard-only on the tri-state and the sheet primary, and an offline edit sent with "Sincronizar agora". It re-checked PR #39 (`7eb8f75`) on the same stack rebuilt at contract version 3, at 768 and 390, and ran a mutation of the press hold. Measured on main before #39: J0 1 tap; J5 14 taps / 44 keys, 0 Voltar (11 after #39); J1 typed 21 / 72, 0 lost; J3 7 / 49; J2 2 taps, 0 keys; J4 4 taps; J6 3 taps, about 7 s. The retro itself ran no browser.

## Previous-retro follow-through

Headless run: transitions are proposed only; no status was written. Epic 6 batch F owns E3-A4, E3-A9, E4-A7, E5-A2, E5-A4, E5-A5 and E12-R2 (`epic-6-context.md`); none is marked done here.

- `epic-5-retro-item-37-e5-a1-…`: already `done` (#34, `07ddc32`).
- `epic-5-retro-item-38-e5-a2-…`: partly landed. The sheet's taps (bulk, Repetir, "Outro…") now run on the durability matrix across Android emulation and WebKit (`lost-taps.durability.spec.ts`); the unit chip row, the sticky bar with the keyboard and the manual device script are not. Proposed: `in-progress`. Owned by Epic 6 batch F.
- `epic-5-retro-item-39-e5-a3-…`: partly landed. Matheus's answers and the 2026-09-24 `source-deltas.md` rows settle OQ-4 (collapse never with an out-of-limit reading, D-2), OQ-5 ("Igual à" never copies per-unit fields, D-3) and OQ-6 (nothing preselected, three reasons plus Outro). OQ-1..OQ-3 and the carried questions are open. Proposed: `in-progress`.
- `epic-5-retro-item-40-e5-a4-…` and `-41-e5-a5-…`: not landed (G-2). Proposed: none. Owned by Epic 6 batch F.
- `epic-5-retro-item-42-e5-a6-…`: applied for the one new refusal (G-3); no new table or wide control. Proposed: none (standing DoD line).
- `epic-5-retro-item-43-e5-a7-…`: applied throughout (P-2). Proposed: none (standing rule).
- `epic-4-retro-item-35-e4-a8-…`: landed again (integrated human-style QA on clean main, `c0ce7b0`, `22f2d29`). Proposed: `done`, as the Epic 5 retro proposed.
- `epic-4-retro-item-36-e4-a9-…`: the matrix ran at close again; no watchdog. Proposed: `in-progress`.
- `epic-4-retro-item-34-e4-a7-…`: not landed; `setup-surface.tsx` grew (G-2). Proposed: none. Owned by Epic 6 batch F.
- `epic-3-retro-item-21-e3-a4-…`: in progress (#34 addressed the timing flakes, 3.6-E2E-001 recurred in batch D). Proposed: stays `in-progress`. Owned by Epic 6 batch F.
- `epic-3-retro-item-27-e3-a10-…`: `git worktree list` shows 9 entries (26 at the Epic 5 retro). Proposed: stays `in-progress`.
- `epic-2-retro-item-16-e2-a4-…`: every 12.x story has `@p0` e2e, and the J3 narrowing carries dated strikes. Proposed: stays `in-progress`.
- `epic-1-retro-item-1-a1-…`, `epic-3-retro-item-18-e3-a1-…`, `-19-e3-a2-…`, `-23-e3-a6-…`, `-26-e3-a9-…`, `epic-4-retro-item-32-e4-a5-…`, `-33-e4-a6-…`: no evidence in Epic 12. Proposed: none. E12-A3 adds the tablet keyboard check to E1-A1.

## Action items

All items are proposals. Code items go through the normal dev loop; product and spec items wait for Matheus.

1. **E12-A1 (product decisions)**: Add the Epic 12 questions to the E5-A3 session: D-2 residue (fold complete sections on a stepper tap or "Próxima ficha"?), D-4 residue (may a stale "Próxima ficha" label conclude and write the suggested instruments?), Bruno's authored copy ("Ficha completa", "falta ⟨campo⟩", the cabine count wording, the pre-issue row, the "Equipamento inacessível" justification), the printed OBSERVAÇÕES with NC items, the TAG field's final form and the status on generate (J-19). Owner: Matheus (Bruno for wording).
2. **E12-A2 (ledger, before Epic 7)**: Add the prefilled-TAG deferral to `deferred-work.md` and to the Epic 7 context: the TAG is never written as an op, so the printer and the `last_nameplate` projection must read `nameplateTagPrefill` (G-1). Owner: coordinator.
3. **E12-A3 (device check)**: Add to the E1-A1 manual device script the soft keyboard after "Outro…" and after "Criar" (E12-Q7) and the lost-tap paths on a real tablet. Owner: Matheus.
4. **E12-A4 (seed upgrade rule)**: Every seed version bump names how existing companies reach it; decide whether the upgrade runs on API start rather than only in the seed CLI, and before a seed version that changes template block shape, reject a content put whose base predates the `seed_version` change (E12-Q4, E12-R1). Owner: architect / dev.
5. **E12-A5 (process, story readiness)**: Recompute every numeric AC target (taps, keys, times) against the decisions of the document it comes from before a story is marked ready (P-4, E12-Q5). Owner: coordinator / SM.
6. **E12-A6 (process, gate and agents)**: New `@p0` specs wait on the target's committed state, never on "non-empty"; a mechanism fix ships with a mutation run proving the gate goes red without it; the QA prompt asks for the report inside its worktree; agents block on background commands instead of sending interim notifications; one coordinator per checkout, a second session uses its own worktree and compose project (P-6, P-7). Owner: coordinator (batch instruction file).
7. **E12-A7 (visual follow-ups, with Epic 6's sheet work)**: Capture 1024 landscape for the v0.9 surfaces (12.5 AC2), fix the stepper double underline and re-check the section 9 header at 390, clear the deferred batch D lows, and note in `MOCK-GUIDE.md` that `key-equipment-sheet.html` and `40-relatorio-overview.html` now render with v0.9 CSS and the "before" exists only as the review's screenshots (P-8). Owner: dev, coordinator for the MOCK-GUIDE note.

## Acceptance verdict

**Accepted with open items**, against the declared criteria (the Epic 12 ACs and DoD in `epics.md`, read with the dated J3 amendment of `c0ce7b0`).

- Every story is `done`; `pending_stories` is empty.
- Per the integrated review, every AC passes on main; 12.3 DoD and 12.6 AC1 pass as amended (J3 7 / 47 instead of 5 / 40, dated, with the D-3 reason). The two "pass with defects" rows (12.2 AC6, 12.4 AC2) and the only high defect (E12-Q1) were fixed in #39 and re-checked.
- The review's targets were met or beaten: J0 1 tap, J1 copied plate 9 / 36 (target at most 12), 0 lost taps, J2 0 keys.
- Open items: the product questions (E12-A1), the tablet keyboard evidence (E12-A3, E1-A1), the Epic 7 TAG dependency missing from the ledger (E12-A2), the seed upgrade rule (E12-A4) and visual lows (E12-A7). None corrupts a sheet in the tested flows; six of the #39 fixes lack an independent re-check.

## Open questions

1. D-2 and D-4 residues and Bruno's authored copy (E12-A1).
2. Does the standard-template upgrade run on API start, or only when the seed CLI runs (E12-R1)?
3. Is the 1024 landscape capture still required by 12.5 AC2, or is 768 and 1280 enough?

## Assumptions

- Epic 12 was taken from the invocation (`-H epic 12`); `detect-epic --epic 12` returned `story_count: 6` and `pending_stories: []`.
- The machine verdict `accepted-with-open-items` was rendered on the evidence alone, with no human decision.
- By the invocation's token-economy instruction, `bmad-review` was not run on the diff, no subagent, browser or test ran, and only the listed evidence was read; the aggregate pass was inline and light.
- Every action item above is proposed, not applied; no previous action item's status was changed by this retro, and the Epic 6 carry-over items are left as they are.
- `epic-12` and `epic-12-retrospective` are set to `done` at the invocation's request.
