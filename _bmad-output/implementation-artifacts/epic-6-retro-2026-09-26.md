---
epic: 6
date: 2026-09-26
verdict: accepted-with-open-items
criteria: declared
headless: true
---

# Epic 6 retrospective: Photos and points of attention

## Epic summary

- Epic 6 covers stories 6.1 to 6.6. All six are `done` in `sprint-status.yaml`; `detect-epic --epic 6` returned `story_count: 6`, `pending_stories: []`. The epic also carried a batch F of earlier-epic action items, which Matheus asked to close here so they do not travel further.
- Diff range: `e9e863c..ec506ab`, 283 files, +18281 / -2673, every PR squash-merged. Contract version 4, seed v3.

  | PR | Scope | Merge commit |
  |---|---|---|
  | #40 | batch F: carry-over E3-A4, E3-A9, E4-A7, E5-A2, E5-A4, E5-A5, E12-R2 | fc4ff89 |
  | #41 | 6.1 + 6.2 burst photos, context captions, durability (batch P1) | de0520d |
  | #42 | 6.6 points of attention and section 8 entries (batch P3) | 8daa8dc |
  | #43 | 6.3 + 6.4 + 6.5 gallery, import, caption composer (batch P2) | fc4d5a1 |
  | #44 | integrated review fixes E6-Q1..Q6, Q8, Q10..Q14, stale "Confirmar" (batch Q) | 43353d9 |
  | #45 | parallel e2e infra, merged into #44's branch by mistake | (not on main) |
  | #46 | #45 re-landed on main: per-worker companies, leak check, parallel workers opt-in | b4bf96d |

- Coordinator commits on main: `f9abb84` (carry-over action items marked after #40), `324d8c3`, `9db178a`, `a39e67a` (status flips), `b7ac7b5` (integrated review and QA), `5f2019d` (sync push slowdown deferred), `ec506ab` (re-check, E6-R1/R2 deferred, coordinator notes).
- Evidence used: `reviews/epic-6-coordinator-notes.md` (delivery shape, per-batch tokens and times, W-1..W-5, B-1..B-7), `reviews/epic-6-review-qa.md` (E6-Q1..Q14, per-AC table, open questions, re-check with E6-R1/R2), `epic-6-context.md` (carry-over list, coordinator decisions), the Epic 6 section of `epics.md` (lines 1611-1766, grep only) and its shared Definition of Done (lines 415-428), `git log e9e863c..HEAD`, `git diff --shortstat`, `deferred-work.md` entries added in the range (grep), `sprint-status.yaml`.
- Missing evidence and narrowed scope:
  - The orchestrators' session logs and the PR bodies were not read (token economy); timings and process lessons come from the coordinator notes and the review.
  - `bmad-review` was not invoked on the diff, by the invocation's token-economy instruction; the integrated review covered the product diff story by story with cross-batch lenses. The retro's own pass was a light inline aggregate check (file growth, @p0 per story, ledger states, dated narrowings in `epics.md`), no subagent.
  - The retro ran no browser and no test. The P1 coverage percentage of DoD clause 3 was not computed; the proxy is `test:e2e:full` green on #44 (1152 s) and the re-check.

## Findings

### Fixed during the epic

| ID | Finding | Source | Disposition |
|---|---|---|---|
| E6-Q1 (medium) | At 768 a `@p1` tap journey hit `button.rail-toggle` over "Criar “Fabricante J1”": scroll anchoring from the header's "Preenchido por" line closed the popover. Reached main from #41 because batches ran only `verify` (@p0). | review E6-Q1 (bisect `fc4ff89` green, `de0520d` red) | fixed in #44 (`ficha-header.tsx`, unit test); re-check 12.1-E2E-009 and 12.3-E2E-004 3 of 3 |
| E6-Q2 (medium) | The point editor lost typed text on Esc or reload, against FR-61 and the EXPERIENCE autosave rule. | review E6-Q2; `deferred-work.md` entry closed 2026-09-25 | fixed in #44 (autosave per field, drafts on hide, "Cancelar" removed); re-checked. Residue E6-R1 |
| E6-Q3 (medium) | Caption composer at 390: 35 chips, "Salvar legenda" about 1100 px below, no photo shown. | review E6-Q3 | fixed in #44 (pinned preview, sticky bar); re-checked `R1-composer-390.png` |
| E6-Q4 (medium) | "De qual equipamento?" was a flat list of 95 radios with "Geral" last. | review E6-Q4 | fixed in #44 (5 likely rows, "Outro equipamento" grouped tree, "Geral"); re-checked `R2` |
| E6-Q5 (medium) | Two seccionadoras in one cabine printed the same caption. | review E6-Q5; coordinator decision "captions carry TAG and column" | fixed in #44 (kernel `caption.ts`); re-checked. Period and gender agreement stay open for Bruno (E6-A4) |
| E6-Q6 (medium) | Focus return after "Concluir fotos" raced the tile rows (failed alone 1 of 2). | review E6-Q6 | fixed in #44 (`restoreFocus` with watch frames); 3 of 3 |
| E6-Q8, Q10, Q11, Q13, Q14 | Import held the batch in memory; tile without `aria-describedby`; Remover silent about citing points and NC row without a point mark; drag hint on phone; a photo waited 46 s for the 60 s sync timer. | review | fixed in #44; Q8, Q11, Q13, Q14 re-checked in the browser, Q10 by the fix agent's tests only |
| E6-Q12 | Gallery header counts and the toast sentence were composed in web (AD-13). | review E6-Q12 | fixed in #44 by the fix agent's tests; not re-checked by QA |
| Stale "Confirmar" | A stale conclusion confirm wrote nothing and said nothing. | review open question 5; coordinator decision | fixed in #44 ("O texto mudou; confira e confirme de novo", authored), unit-tested only |
| E12-R2 | 12.1-E2E-007 could pass without the press hold. | `deferred-work.md` entry closed 2026-09-25 | fixed in #40 (three rounds) |

### Open: carried from the review and the re-check

| # | Lens | Finding | Source | Disposition |
|---|---|---|---|---|
| E6-R1 | durability (FR-61) | Text typed into a new point right before a plain reload (no page-hidden event) is lost; the draft is written only on hide. | review re-check; `deferred-work.md` (owner Epic 7 carry-over batch) | E6-A6 |
| E6-R2 | UX | Photos captioned before #44 open the composer in "Editar texto"; a chip shows pressed and the caption does not change. | review re-check; `deferred-work.md` (owner Epic 7 carry-over batch) | E6-A6 |
| P2 known-open | UX / test | Sumário "aguardando envio" also counts photos shown "com erro"; no test on which picture the viewer shows; HEIC only mocked. | coordinator notes batch P2 "Known open" | E6-A6 |
| Durability | device risk | Photo durability is untested on WebKit (its contexts refuse Blobs in IndexedDB, the spec skips it); the camera burst and the soft keyboard after programmatic focus are unchecked on a real tablet. DoD clause 1 requires the physical device check at the close of Epic 6 (TC-12). | coordinator notes B-7; review matrix row (8 skipped) | E6-A5, E1-A1 |
| Section 8 | print | Section 8 prints nothing yet; the merged bullet 4 (`groupDerivedPoints`) and the photo token resolution are Epic 7's renderer. | `deferred-work.md` "Section 8 prints nothing yet" (owner Epic 7) | owned by Epic 7; E3-A9 stays in-progress on it |
| Product | product | Manual points naming an equipment (E6-Q9, kept "Geral"), caption period and agreement, row 8 counts reading the derived entry among "pontos", four authored texts, the `criterion_override` shape `{raw, unit}`, an edited conclusion text unguarded against a moved basis, the point editor without "Cancelar", photos never blocking "Gerar" (reversible). | review open questions 1-6; coordinator notes B-6 and batch Q narrowings; `epic-6-context.md` coordinator decisions | E6-A4 |

### Open: gate time (Matheus: "mais de 30 min não é razoável")

| # | Finding | Source | Disposition |
|---|---|---|---|
| T-1 | `verify` never met the 15-minute budget of DoD clause 3: batch F 17m09s; QA 20m37s (lint 39 s, static 70 s, unit 170 s, api 160 s, e2e 798 s); #44 1056 s (17.6 min); #46 1157 s (19.3 min). | coordinator notes table; review "What ran" | E6-A1 |
| T-2 | A fix batch that runs `verify` and then `test:e2e:full` spends 1056 s + 1152 s = about 37 min per merge attempt; batches needed two runs each; batch Q took 474 min, most of it on six full-suite validation runs. | coordinator notes B-1, batch Q row | E6-A1, E6-A2 |
| T-3 | Parallel e2e validation (#45, 3 serial vs 3 parallel full runs): serial 1272 / 1255 / 1281 s, all green; 3 workers 791 / 796 / 812 s (about 37 % faster), but run 1 failed 12.3-E2E-004, never serially. The gate stays at 1 worker (`PARALLEL_WORKERS = 1`), `--workers=3` is opt-in. | coordinator notes batch Q row, W-4; `deferred-work.md` "POST /api/sync/ops slows" | kept at 1 worker, correctly |
| T-4 | Probable cause of the parallel failure: `POST /api/sync/ops` takes about 1.2 s alone and about 17 s median when pushes overlap. This is the next gate action. | `deferred-work.md` (owner Epic 7 carry-over batch; "blocks the 18 -> ~10 min gate") | E6-A1 |
| T-5 | Two `verify` runs at once on 8 cores / 15 GB pushed e2e past 16 min and produced load flakes (5.8-E2E-002, 6.1-E2E-002); E4-A9's "at most two concurrent verifies" is too loose for this machine. | coordinator notes B-2; review "What ran" | E6-A2 |
| T-6 | The new runner (`scripts/e2e.ts`, per-worker companies, leak check, `--workers` opt-in) is not documented in AGENTS.md "Running and verifying" (grep for `scripts/e2e`, `workers`: no match). | coordinator notes batch Q "Open"; `package.json:14-16` | E6-A3 |

### Open: aggregate views (retro pass)

| # | View | Finding | Source | Disposition |
|---|---|---|---|---|
| G-1 | size growth | `setup-surface.tsx` was split (957 lines at `e9e863c` to 234), closing E4-A7; but `ficha-surface.tsx` grew again, 578 lines at the Epic 12 retro to 704, the next god-component candidate. | `git show e9e863c:…` vs `wc -l` at HEAD; Epic 12 retro G-2 | E6-A7 |
| G-2 | spec reconciliation | Epic 6 has more than ten narrowings (composer as modal, recents device-only, gallery camera and header count not built, gallery shot "Geral", no restore list, no day headers, no "Cancelar", nameplate tile to Epic 8, Export warning row to Epic 7, geolocation pref to Epic 11), all recorded in `deferred-work.md`, none as a dated line under its story in `epics.md` (no strike or 2026-09-25/26 date in lines 1611-1766 other than the Dev model lines). | `deferred-work.md` entries added in the range; `epics.md` 1611-1766 grep | E6-A7 (and E2-A4, E4-A5) |
| G-3 | DoD check (E2-A4) | Every Epic 6 story has at least one `@p0` e2e: 6.1 three, 6.2 one, 6.3 one, 6.4 three, 6.5 two, 6.6 eight; eleven more are `@p1`. | grep of `test('@p0 6.x-E2E` in `e2e/` | accept |
| G-4 | ownership | The review's AD-13 lens found one web-side derivation (E6-Q12), fixed in #44; the new rules (`contextCaption`, `numberPhotos`, `derivedPoints`, `pointsSummaryText`, `uploadPillText`, `storageLowBannerText`) are kernel functions. | review E6-Q12; coordinator notes P1/P3 contracts | none (clean after #44) |

### Process

| ID | Finding | Source | Lesson |
|---|---|---|---|
| P-1 | Carry-over first and in parallel worked: batch F closed E3-A4, E4-A7, E5-A4, E5-A5 and E12-R2 in one PR while P1 built photos, and advanced E3-A9 and E5-A2; launch prompts fenced each batch's files. | coordinator notes W-1; `f9abb84` | Keep a carry-over batch first in each epic (E6-A7). |
| P-2 | Cross-batch contracts in PR bodies and resume messages let P1 adopt F's path builders and P2 keep one `numberPhotos`. | coordinator notes W-2 | Keep. |
| P-3 | The integrated pass again found what no batch saw (E6-Q1..Q6); a `@p1` regression reached main because each batch ran only `verify`. | coordinator notes W-3, B-4; review E6-Q1 | E6-A2. |
| P-4 | The parallel-e2e safeguards Matheus asked for (per-worker companies, a serial export group, a leak check, a 3+3 comparison) kept a flaky gate from shipping. | coordinator notes W-4 | Validate any gate change the same way. |
| P-5 | Token economy held: 232k-312k per batch, QA 298k, cached re-check; every agent on opus. | coordinator notes table, W-5 | E5-A7 applied. |
| P-6 | A stacked PR (#45 on #44's branch) merged into that branch instead of main and had to be re-landed as #46. | coordinator notes B-3 | Retarget stacked PRs with `--base main` after the first merges (E6-A7). |
| P-7 | The QA agent again could not write outside its worktree; this time the coordinator copied its report by design. | coordinator notes B-5 | E12-A6 part applied. |
| P-8 | Product and wording questions keep accumulating for Matheus and Bruno. | coordinator notes B-6; review open questions | E6-A4. |

## Behavior verification

The integrated QA agent exercised Epic 6 end to end on an isolated stack (`fasor-qa6`) at `a39e67a` through the Playwright MCP browser at 1280, 768 and 390 px, light and dark: a relatório from the standard template, sticky-bar camera, "Adicionar fotos" on a sheet and in the gallery, EXIF time, GPS and orientation 6, a non-image file, offline capture and "Sincronizar agora", the viewer, captions on desktop and phone, Remover and Desfazer, points from an NC row with a photo token, Não ensaiado with its derived entry, a point citing a removed photo, the Export dialog, the storage-low banner under a stubbed 180 MB estimate, eviction, and the served variant sizes (thumb 512, print 2000). The MCP camera delivers no frames; the live burst is covered only by 6.1-E2E-001 on a fake device. The re-check at `b4bf96d` covered E6-Q1..Q6, Q8, Q11, Q13, Q14 and the stale "Confirmar" (code only), and ran the four flaky specs 12 of 12. The retro itself ran no browser.

## Previous-retro follow-through

Batch F and batch Q were the carry-over owners. The coordinator marked the batch F items after #40 (`f9abb84`); this retro checked them against the coordinator notes and the review, and they are consistent. At the invocation's request (Matheus asked for the carry-over to be settled in this epic) the retro writes one further transition; every other line below is a proposal.

Resolved or advanced by batch F or batch Q (status in `sprint-status.yaml` after this retro):

- `epic-3-retro-item-21-e3-a4-…`: `done` (#40). 3.6-E2E-001 was green in verify and full; unit 170 s.
- `epic-4-retro-item-34-e4-a7-…`: `done` (#40). `setup-surface.tsx` 957 to 234 lines (G-1).
- `epic-5-retro-item-40-e5-a4-…`: `done` (#40); the stale "Confirmar" message landed in #44; E5-Q18 (b) moves to Epic 7.
- `epic-5-retro-item-41-e5-a5-…`: `done` (#40), `packages/domain/src/ops/path.ts`.
- `epic-3-retro-item-26-e3-a9-…`: `in-progress`. Everything landed in #40 except the merged section 8 bullet 4: its data half (`groupDerivedPoints`) landed in #42, the renderer is Epic 7.
- `epic-5-retro-item-38-e5-a2-…`: `in-progress`. The sheet specs run in `test:e2e:matrix` (`@p1`, matrix 58 passed); the manual device run is Matheus's (E1-A1).
- `epic-4-retro-item-36-e4-a9-…`: `open` to `in-progress` (written by this retro). The matrix ran at epic close (478 s, EXIT 0); the concurrency rule is replaced by one `verify` at a time (T-5, E6-A2); the parallel runner infra landed in #46; no shared SyncState factory or watchdog evidence.
- E12-R2 (a finding, not an action item): closed in `deferred-work.md` by #40.

Proposed only (not written):

- `epic-4-retro-item-35-e4-a8-…`: integrated human-style QA on clean main ran again (`b7ac7b5`, `ec506ab`) and the fix agents reported product choices as open questions (batch Q "For Bruno"). Proposed: `done`, as the Epic 5 and Epic 12 retros proposed.
- `epic-2-retro-item-16-e2-a4-…`: the @p0 half holds (G-3); the dated narrowings half does not (G-2). Proposed: stays `in-progress`.
- `epic-5-retro-item-43-e5-a7-…`: applied again (P-5). Proposed: `done` once the shape is folded into a tracked orchestrator file (the Epic 6 instruction file lives outside the repo).
- `epic-12-retro-item-45-e12-a2-…`: the ledger half landed (`deferred-work.md:749`, added in `e9e863c`); the Epic 7 context does not exist yet. Proposed: `in-progress`.
- `epic-12-retro-item-49-e12-a6-…`: the QA report handling is by design now (P-7); no mutation run or single-coordinator evidence. Proposed: stays `open`.
- `epic-12-retro-item-50-e12-a7-…`: not picked up by Epic 6 although its text says "with Epic 6's sheet work". Proposed: stays `open`, re-pointed to Epic 7's carry-over batch.
- `epic-3-retro-item-27-e3-a10-…`: `git worktree list` shows 13 entries (9 at the Epic 12 retro). Proposed: stays `in-progress`.

Open items that remain, and why:

- Human-only, for Matheus (and Bruno for wording), no agent can close them: E1-A1 (manual iPad and Android offline proof, now also required by TC-12 at the close of Epic 6, with E12-A3's keyboard checks and E6-A5's photo checks), E3-A1 (R-009 seed review record, D-2/D-3/D-6), E3-A2 (`not_tested_reason` of the Porto Seguro untested blocks), E3-A5 (D-5 and D-7, with Winston), E3-A6 (seed/fixture freeze rule), E4-A5 (dated narrowings and the Rascunho-generates decision), E4-A6 (Bruno reads the skeleton DOCX), E5-A3 (OQ-1..OQ-6 and the carried questions), E12-A1 (Epic 12 product questions), E12-A3 (tablet keyboard and lost taps), E3-A10 (stale worktrees, Matheus's tooling), E2-A4 (Matheus's DoD reconciliation).
- Architecture or later epics: E1-A12 (exposure hardening, Epic 11 checklist), E12-A4 (seed upgrade rule, before the next seed that changes block shape).
- Standing process rules, applied each epic but open until folded into a tracked instruction file: E4-A8, E5-A6, E5-A7, E12-A5, E12-A6.
- Deferred to Epic 7's carry-over batch: E12-A2 (Epic 7 context half), E12-A7 (visual follow-ups), E3-A9 (bullet 4 renderer).

## Action items

All items are proposals. Code items go through the normal dev loop; product and spec items wait for Matheus.

1. **E6-A1 (gate time, next action)**: Fix the sync push slowdown under overlap (`POST /api/sync/ops` about 1.2 s alone vs about 17 s median; apply a push batch in one transaction or find the lock or pool the pushes serialize on), repeat PR #45's 3 serial vs 3 parallel full-run validation, and switch the gate to 3 workers if it holds, bringing `verify` back under the 15-minute budget (measured 17-21 min; e2e 12-13 min serial, full runs about 13 min on 3 workers). Owner: Epic 7 carry-over batch (dev).
2. **E6-A2 (gate policy)**: One `verify` at a time on this machine (replaces E4-A9's "at most two"); a batch that touches the sheet, overlays or shared layout also runs `test:e2e:full` before merge, and the epic's last story PR always does (review open question 7). Owner: coordinator; Matheus decides the `e2e:full` rule.
3. **E6-A3 (docs)**: Document the new e2e runner in AGENTS.md "Running and verifying": `scripts/e2e.ts`, per-worker companies, the serial export group, the leak check and `--workers` opt-in. Owner: coordinator.
4. **E6-A4 (product decisions)**: Add to the E5-A3 session: manual points naming an equipment (E6-Q9), caption period and gender agreement, row 8 counts with the derived entry, the four authored texts ("Ponto de atenção n", the Remover line, "N fotos ficaram como Geral, sem legenda", "O texto mudou; confira e confirme de novo"), the `criterion_override` shape `{raw, unit}`, an edited conclusion text not guarded against a moved basis, the point editor without "Cancelar", and confirm or reverse "photos never block Gerar". Owner: Matheus (Bruno for wording).
5. **E6-A5 (device check, TC-12)**: Add to the E1-A1 manual device script a camera burst and an import on a real tablet, photo durability on iPad Safari (untested in Playwright WebKit, which refuses Blobs in IndexedDB), and the soft keyboard after programmatic focus. Owner: Matheus.
6. **E6-A6 (Epic 7 carry-over lows)**: Persist the point draft on each input, debounced (E6-R1); make a chip tap in "Editar texto" either regenerate with a notice or show the chips inactive (E6-R2); stop counting "com erro" photos as "aguardando envio" on the Sumário; add a test on which picture the viewer shows. Owner: Epic 7 carry-over batch (dev).
7. **E6-A7 (process and structure)**: Retarget stacked PRs with `--base main` after the first merges (P-6); every narrowing written to `deferred-work.md` also gets a dated line under its story in `epics.md` (G-2); split `ficha-surface.tsx` (704 lines) when next touched (G-1); keep the carry-over-first batch in each epic. Owner: coordinator (batch instruction file), dev for the split.

## Acceptance verdict

**Accepted with open items**, against the declared criteria (the Story 6.1-6.6 ACs in `epics.md` 1611-1766 and the shared Definition of Done, `epics.md` 415-428).

- Every story is `done`; `pending_stories` is empty.
- Per the integrated review, every AC passes on main; the five "pass with" rows (6.1 AC3, 6.4 AC1, 6.4 AC3, 6.5 AC1, 6.6 AC1) were fixed in #44 and re-checked, except E6-Q9, kept as "Geral" by a coordinator decision open for Matheus. The carry-over rows (E3-A4, E3-A9 as scoped, E4-A7/E5-A5, E5-A2, E5-A4, E12-R2) pass.
- #44 reports `verify` and `test:e2e:full` green; the re-check ran the four flaky specs 12 of 12.
- Unmet DoD clauses, recorded rather than waived: clause 3's 15-minute budget (every measured `verify` 17-21 min, E6-A1), and clause 1's physical device check at the close of Epic 6 (TC-12; E1-A1, E6-A5). Neither corrupts a relatório in the tested flows; both need Matheus or the next batch.

## Open questions

1. Should `test:e2e:full` run before an epic's last story PR, or before every batch that touches shared layout (E6-A2)?
2. Is a gate at 3 workers acceptable once the push slowdown is fixed and a new 3+3 validation is green, and what wall time does Matheus accept for `verify` meanwhile?
3. The product questions of E6-A4.

## Assumptions

- Epic 6 was taken from the invocation (`-H epic 6`); `detect-epic --epic 6` returned `story_count: 6` and `pending_stories: []`.
- The machine verdict `accepted-with-open-items` was rendered on the evidence alone, with no human decision.
- By the invocation's token-economy instruction, `bmad-review` was not run on the diff, no subagent, browser or test ran, and only the listed evidence was read; the aggregate pass was inline and light.
- The skill's headless rule is to propose previous-item transitions only. The invocation carried Matheus's explicit request to settle the carry-over in this epic, so the retro wrote one transition (E4-A9 `open` to `in-progress`) and left the batch F items as the coordinator had set them in `f9abb84`, having found them consistent; every other transition above is a proposal.
- Every Epic 6 action item above is proposed, not applied.
- `epic-6` and `epic-6-retrospective` are set to `done` at the invocation's request.
