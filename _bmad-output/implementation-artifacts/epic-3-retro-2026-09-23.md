---
epic: 3
date: 2026-09-23
verdict: accepted-with-open-items
criteria: declared
headless: false
---

# Epic 3 retrospective: Seed data and Templates

## Epic summary

- Epic 3 covers stories 3.1 to 3.7. All seven are `done` in `sprint-status.yaml`, and `detect-epic --epic 3` returned `pending_stories: []`.
- Diff range: `945e8d4^..e0b4159`, 11 commits, no merge commits. That covers the four story PRs plus the two fix PRs from this review. `git_evidence.py` shows the largest additions in `fixtures/porto-seguro/data.ts` (+987), `e2e/templates.spec.ts` (+943), `template-composer.test.tsx` (+747), `seed/v1.ts` (+664) and `fixtures/porto-seguro/op-log.ts` (+626).

  | PR | Stories | Merge commit |
  |---|---|---|
  | #18 | 3.1 + 3.2 | 1bab45a |
  | #19 | 3.3 + 3.4 | acae360 |
  | #20 | 3.7 | cfda654 |
  | #21 | 3.5 + 3.6 | f3cbe10 |
  | #22 | review fixes, kernel and fixture (K1-K10) | 743ce40 |
  | #23 | review fixes, web (W1-W10) | e0b4159 |

- Evidence used:
  - `reviews/epic-3-review-qa.md`. Two independent code reviews (opus) covered the kernel side and the web side of `945e8d4..c6d1db7`. A human-style Playwright pass ran on the isolated stack `fasor-qa3` at 1280, 768 and 390, in light and dark, with screenshots `reviews/qa-epic-3/01..21`. The document also records the fix status.
  - The specs `spec-3-1-3-2-*.md`, `spec-3-3-3-4-*.md`, `spec-3-5-3-6-*.md` and `spec-3-7-*.md`.
  - `deferred-work.md`, the Epic 2 retro, and PRs #22 and #23 with their verify output.
- Missing evidence:
  - The build sessions' logs were not read, so the process lessons come from the artifacts and PR history.
  - `bmad-review` was not invoked separately. The two independent reviewers stand in for its lenses, so that part of the review has a narrower scope.

## Findings

Each finding was re-checked against its primary source. The full text and reproduction steps are in `reviews/epic-3-review-qa.md`.

### Fixed during this review

| ID | Lens | Finding | Source | Disposition |
|---|---|---|---|---|
| K1 | spec-to-implementation | The fixture sorts locations alphabetically, and the golden snapshot froze that order. | `fixtures/porto-seguro/op-log.ts:222,232` (before #22) | fixed in #22, with order tests |
| K2, K3 | spec-to-implementation | Section 8 prints the not-tested points first. The "verbatim" bullets lost their `;`. Captions carry "Imagem NN:". | `op-log.ts:478-493,519`; `data.ts:166-171` | fixed in #22 |
| K4 | pattern divergence | `not_tested.reason` stored a pt-BR label; the spine says it holds a seed key. | `data.ts:173`; spine line 169 | fixed in #22 (`solicitacao_cliente`); the choice of reason is open as E3-A2 |
| K5 | ownership | The quantity maximum, unique `na_defaults` and non-blank names lived only in UI helpers. | `schemas/block-config.ts` (before #22) | fixed in #22 |
| K6-K10 | edge case / hygiene | These covered the plural "1 cabos", unknown `{tokens}` passing without a report, client names in test files, stale ledger entries and a tautological determinism test. | review K6-K10 | fixed in #22 |
| W1 | a11y | Focus fell to `<body>` after archive, restore, undo and every removal. | `templates-surface.tsx:210,239`; `template-composer.tsx:250-316` (before #23) | fixed in #23, with focus assertions in `@p0` e2e |
| W2, W3 | edge case | In the section editor, Ctrl+Z undid the wrong edit. Repeated spaces collapsed on reopen. | browser pass, `qa-epic-3` s19-s22 | fixed in #23 (3.6-E2E-003) |
| W4-W10 | UX / mock parity / gate | Locked toggles had the wrong markup. Duplicate names collided. A toast leaked into the composer. The phone inset was missing. Tapping a label did nothing. The axe test timed out. Holding a key sent one op per repeat. | review W4-W10 | fixed in #23 (W5 by the coordinator, `ef27e49`); the rest of W4 is open as E3-A9 |

### Open

| ID | Lens | Finding | Source | Disposition | Upstream lesson |
|---|---|---|---|---|---|
| D-1 | verification gap / human gate | Story 3.1 requires Matheus's R-009 review of the seed against the decoded FO.SERV-03, recorded with its date "before Story 4.1 starts". No record exists, yet v1 is hash-frozen and 3.1 is `done`. | `epics.md` Story 3.1, R-009 clause; no dated record under it | fix now (blocks Story 4.1) | A freeze requires the human review record first. |
| D-2 | spec-to-implementation | `{obra}` is both the site phrase in section 1 and the cover's Cidade/local, and "da" is hard-coded. Section 1 cannot reproduce the delivered sentence. | `seed/sections-v1.ts:40`; `COVER_V1` | fix now as seed v2, or unfreeze v1 before the first relatório (Matheus decides) | Goldens are asserted against the source document. |
| D-3 | domain | The subtype NA defaults are smaller than the fixture's real pattern: seccionadora 4 vs 2, dry-type 10 vs 8. The standard template sets no subtype on TP, TC or transformer. | `fixtures/porto-seguro/data.ts:67-114`; `seed/template.ts:69` | decide in the R-009 review (D-1) | The same as D-1. |
| D-4 | architecture | `template.version` is immutable, so every relatório will record `template_version = 1`. | `ops/path.ts` `IMMUTABLE_KEYS`; spine line 89 | decide before Epic 4's `instantiateTemplate` | none |
| D-5 | architecture | The two contact-insulation tables share one test key and are told apart by a row offset that only the fixture defines. | `op-log.ts:354-362` | define it in the kernel before Epic 5's renderer | Conventions a renderer needs live in the kernel. |
| D-6 | domain | Roles are inconsistent in the standard template. The PTMT/IM panels and the PTMG columns are flattened. | `seed/template.ts:103-150` | decide in the R-009 review | none |
| D-7 | architecture | Template edits put the whole `blocks`/`skeleton` array, so the last writer wins. An undo re-puts an old array over pulled edits. | outbox paths seen in the browser; `template-composer.tsx:178-198,380-389` | accept for the office-only MVP; revisit if templates are edited on two devices | none |
| G-1 | gate stability | `@p0 3.6-E2E-001` failed once: the Templates list rendered no row within 30 s after sign-in, at load average ~5. The rerun passed 36/36. PR #22's first verify also failed on a load timeout. | PR #23 body, coordinator addendum; PR #22 body | fix now (investigate the first-pull latency under load) | The gate must not depend on machine load. |
| G-2 | cross-story | The path-segment validation against `getDefinition` is still open, and it is due before Epic 4 writes sheet values. | `deferred-work.md` (refreshed in #22); `ops/path.ts` | fix now (before Epic 4 sheet writes) | Re-check the ledger when a blocking story ships. |
| P-1 | process | Stories were batched two per PR (#19, #21) against AGENTS.md's one-story-per-PR rule. The 3.5+3.6 spec carries `warnings: ['multiple-goals', 'oversized']`. Its dev model was raised to opus·high with no note in `epics.md`. | `git log`; `spec-3-5-3-6-*.md` frontmatter; `epics.md` 3.5/3.6 Dev model lines | process | Batching needs a recorded exception. |
| P-2 | process | ACs still close only in jsdom or at `@p1`: Restaurar with undo, touch hold and drag, the drawer and sheet. The contenteditable editor never ran on Android or WebKit. | web reviewer's AC table in `reviews/epic-3-review-qa.md` | process | E2-A4 is not yet fully honored. |
| P-3 | tooling | Tooling friction: the Playwright MCP browser profile was locked by another session's long-lived server; 13 stale worktrees remain under `.claude/worktrees`; a fix agent's first compose command hit the default `fasor` project; PR bodies #18-#21 carry an emoji that AGENTS.md forbids. | `git worktree list`; the review's side-effect note; `gh pr view 21` | process | none |

### Accepted as-is

- The rest of W4: the `.toggle-sub` lines for sub-block rows need new kernel text, deferred.
- A blank rename closes the dialog with no message.
- A section card shows its position box next to the section number.
- "Editar texto" is reachable only from the Overflow.
- The kernel fix left section 8 with three per-block not-tested points where the source has one bullet 4. The renderer (Epic 5) should decide how to merge them.

### What went well

- The seed is faithful. It is tested against hand-written golden lists from addendum §9, and it is zod-validated, bound to its criteria, deep-frozen and hash-pinned when the module loads. 119 boilerplate strings match the docx.
- The composer works well by hand: the stepper clamps and announces, holding a button repeats, the reorder announcements match the AC sentence exactly, and the palette follows the selected column.
- The web side kept to the rules. `tokens.css` and `components.css` are byte-identical to the mocks. Each `.frame-*` translation cites its mock rule, and derived text lives in the kernel.
- The Epic 2 improvement "at least one `@p0` per story" held, and a two-device convergence e2e test exists (3.4-E2E-005).
- The review-to-fix loop closed in the same session: two PRs, verify green, and the fixes confirmed again in the browser (`qa-epic-3/21`).

## Behavior verification

On `fasor-qa3` at `c6d1db7`, these flows were exercised end to end:
- the Templates list, with duplicate, archive, restore, and remove with undo;
- the empty state and seeding for Empresa B;
- the composer: add, rename and remove cabines and colunas, reorder by Alt+arrows and by the Position box (including out of range), the stepper (typed, clamped, "—", press-and-hold), a cabine holding blocks directly, the type defaults dialog with its subtypes, and the section text editor (chips, Backspace, paste, Ctrl+Z, whitespace, Restaurar with undo);
- the layout at 390, 768 and 1280, in light and dark.

After #22 and #23 merged, the fixes were checked again on the same stack at `e0b4159`. The per-AC results are in the review document.

## Previous-retro follow-through

Items from earlier retros that are not `done` in `sprint-status.yaml`:

- `epic-1-retro-item-1-a1-run-the-manual-ipad-and-android-offli`: no evidence found. `_bmad-output/test-artifacts/manual/` holds only the template and the script. Proposed status: none (Matheus has to run it; it blocks Epics 5 and 6).
- `epic-1-retro-item-12-a12-before-any-exposure-beyond-the-offic`: not applicable yet. Proposed status: none.
- `epic-2-retro-item-16-e2-a4-dod-every-user-facing-ac-exercised`: partly landed. Every Epic 3 story has a `@p0` test, but several ACs close only in jsdom or at `@p1`, and none was moved in `epics.md` with a date (P-2). Proposed status: `in-progress`.
- `epic-2-retro-item-17-e2-a5-human-style-pass-covers-each-form`: landed for Epic 3. The composer, the dialogs and the palette sheet were checked at 390 (`qa-epic-3/16-19`), and a second-device scenario runs in `@p0 3.4-E2E-005`. Proposed status: `done`, if Matheus confirms. It stays a standing practice.

## Action items

All items are proposals. Code items go through the normal dev loop.

1. **E3-A1 (blocks Story 4.1)**: Run the R-009 review of seed v1 against the decoded FO.SERV-03 and record it with the date under Story 3.1 in `epics.md`. In that review, decide D-2 (`{obra}` split and article), D-3 (the NA defaults and the subtypes of the standard template) and D-6 (roles and flattened panels). Also decide whether they ship as seed v2 or as a deliberate unfreeze of v1 before the first relatório exists. Owner: Matheus.
2. **E3-A2**: Decide which `not_tested_reason` fits the Porto Seguro untested blocks: `solicitacao_cliente`, as stored now, or `impossibilidade_desligamento`, which matches bullet 4's wording. Owner: Matheus.
3. **E3-A3 (before Epic 4 writes sheet values)**: Validate `sheet/*` path segments against `getDefinition` in `applyOp` (G-2). Owner: dev.
4. **E3-A4**: Stabilize the gate (G-1). Find out why the Templates list can render empty for 30 s after sign-in under load, then fix the wait or the first pull. Also cut the unit suite's jsdom and axe cost. Owner: dev.
5. **E3-A5**: Decide D-4 (`template.version`), D-5 (the row offset of the contact-insulation tables in the kernel) and D-7 (array last-writer-wins on templates) before Epic 4's `instantiateTemplate` and Epic 5's renderer. Owner: Winston (architect) with Matheus.
6. **E3-A6 (process)**: A seed or fixture freeze requires the human review record first. Golden files get at least one explicit assertion against the source document (order, text, numbering), not only regeneration. Owner: Matheus / SM.
7. **E3-A7 (process)**: One story per PR, or an exception recorded in the spec. A change of Dev model is noted in `epics.md` with its date. Owner: Matheus / SM.
8. **E3-A8 (process)**: Every destructive action (archive, remove, undo) has an e2e assertion on the focused element. Every contenteditable or touch AC runs at least once on `durability-android-chrome` or WebKit before the story closes. Owner: dev and reviewers.
9. **E3-A9 (deferred polish)**: The `.toggle-sub` lines from kernel text; one merged bullet 4 in section 8 for the renderer; synthetic names in the remaining test files (`home-surface.test.tsx`, `client-panel.test.tsx`, `e2e/home.spec.ts` and others); a message on blank rename; a way to open "Editar texto" from the section card. Owner: dev.
10. **E3-A10 (tooling)**: Remove the 13 stale worktrees under `.claude/worktrees`. Document in the pipeline instructions that a worktree's `.env` must exist before its first `docker compose` command. Drop the emoji from the default PR attribution line (AGENTS.md). Owner: Matheus.

## Acceptance verdict

**Accepted with open items**, against the declared criteria.

All seven stories are `done`. The browser pass and the two reviews show the ACs of 3.2 to 3.7 met on `e0b4159`, including the review fixes from #22 and #23, and `pnpm verify` passed on both fix PRs.

These items stay open:
- The R-009 clause of Story 3.1 (D-1). The AC sets its deadline as "before Story 4.1 starts", so it does not fail the epic yet, but E3-A1 blocks Story 4.1.
- The seed decisions D-2, D-3 and D-6.
- The architecture decisions D-4, D-5 and D-7.
- G-1 and G-2.

If Matheus judges that freezing v1 without the review breaks Story 3.1 now, the verdict becomes **rejected** until E3-A1 lands. A human decision overrides.

## Open questions

- Will seed v1 be unfrozen, or will v2 ship before the first relatório? The answer changes how D-2 and D-3 are fixed.
- Should the Porto Seguro fixture reproduce the delivered document exactly, including its not-tested bullet and reason, or the corrected document the app will produce? The goldens for Epic 5 depend on it.
- Do templates need to be edited on two devices in the MVP? If so, D-7 cannot be accepted.
