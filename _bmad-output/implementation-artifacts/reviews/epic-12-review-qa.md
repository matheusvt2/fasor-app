# Epic 12 integrated review and QA

- **Commit:** `48e3daf33cacfd02b81bd580f30db473121a3dab` (origin/main); review range `144a558..48e3daf`, PRs #35 (12.2), #36 (12.1), #37 (12.3 + 12.4), #38 (12.5 + 12.6).
- **Stack:** isolated compose project `fasor-qa12` (web `:36073`, api `:36030`, Postgres `:36032`, MinIO `:36090/36091`), dev web server (no service worker), Node 24 in the `tools` container; the mocks served read-only by a throwaway `caddy` container on `:36099`. The stack is left up for the re-check.
- **Reviewer:** independent reviewer and QA, 2026-09-25. It reviewed and tested only and changed no product code, test or planning document.
- **Screenshots:** `reviews/qa-epic-12/` in the main checkout (15 files; `M-*` mocks, `J*` journeys, `V-*` visual pass, `O-01` the Sync page after the offline edit was sent).

## What ran

| Run | Result |
|---|---|
| `pnpm verify` | **EXIT 1.** lint and static clean. Unit: 992 tests in 70 files and 790 in 86 files (kernel and web), 20 tooling tests. api: 144 tests in 25 files. e2e `@p0`: 88 tests, **86 passed, 2 failed** in 9.8 min: `@p0 12.2-E2E-001` (J0) and `@p0 12.2-E2E-003` (J5). |
| Re-run alone | `journey-forward.spec.ts --repeat-each 2` on desktop-chrome: 9 passed, **12.2-E2E-001 failed 1 of 2**; 12.2-E2E-003 passed 2 of 2. Both passed in `test:e2e:full`. Flaky tests, not product defects (E12-Q3). |
| `pnpm test:e2e:full` | **EXIT 0.** 151 tests: 147 passed, 4 skipped (the three touch tests and the `CAPTURE_P=1` capture spec), 13.8 min. |
| `pnpm test:e2e:matrix` | **EXIT 0.** 48 tests: 41 passed, 7 skipped, 5.2 min. `lost-taps.durability.spec.ts` (12.1-E2E-001..006) is green on durability desktop Chrome, Android Chrome emulation and WebKit. |
| CSS promotion | `cmp` of `mockups/tokens.css` and `mockups/components.css` against `apps/web/src/styles/`: byte-identical. The three new `.frame-phone .sum-row` rules have their `app.css` translation. `DESIGN.md` is at version 0.9.0. The `-v09.css` files are folded in and removed. |
| Code review | The product diff story by story, read with the brief's lenses: press-hold, ownership, "nothing unconfirmed is written", seed v1 against v2, not-tested sheet, two sheets of one cabine, CSS and a11y. The four spec triage logs and the coordinator log were checked item by item (table below). |
| Browser pass | Playwright MCP (desktop Chrome), company A, through the UI. A relatório created from nothing ("Condomínio Teste · Torre Norte", standard template, seed v2, no instrument registered). Journeys J5, J1 (SEC-ENEL, plate typed), J3 (SEC-ENEL-2), J2 (TP-ENEL), J4 (TC-ENEL), J0 and J6. Taps and keys were counted by a page-level `pointerdown`/`keydown` listener plus 2 taps per native select. Widths 390, 768 and 1280, light and dark, a short 768 x 520 viewport, keyboard-only on the tri-state and the sheet primary, offline edit then "Sincronizar agora" (sent, badge back to "sincronizado"). The v0.9 mocks were compared at 768 (sheet) and 390 (Sumário). |

## Findings

| ID | Sev | Kind | AC | Where | Reproduction / evidence | Disposition |
|---|---|---|---|---|---|---|
| E12-Q1 | **high** | defect (pre-existing since the sheet's registry fields, reached by the shortened J1 path) | 12.4 AC2 ("Criar “⟨valor⟩” still creates the registry entry"), J-09 | `apps/web/src/components/registry-picker-field.tsx:115-118` (`setInputValue(text.trim())`); `apps/web/src/surfaces/ficha/ficha-fields.tsx:308` (`parseVoltageClassKv`) | On SEC-ENEL, tap Tensão de placa "Outro…", type **"15 kV"** and tap "Criar “15 kV”". The registry row is created as **"15"** and `nameplate/tensao_de_placa "15"` is written. The input keeps showing "15 kV", so on the next blur the Combobox finds no match and writes **`tensao_de_placa null`** (outbox, in that order). The field still reads "15 kV" while the header says "Falta 1 campo da placa". The value the engineer sees is gone, and nothing on screen says which field is missing. Screenshot `J1-02-tensao-criar-not-written.png`. Typing "15" does not lose it. | **Fix now:** after a create, set the input to the created row's label (the parsed name), not the typed text. Add a 5.x or 12.4 e2e that types a unit ("15 kV") and asserts the cell survives a blur. |
| E12-Q2 | medium | defect (navigation, a11y) | 12.2 (forward path, J-06, J-11); focus after navigation | No `scrollTo` or scroll restoration anywhere in `apps/web/src` (grep). Examples: `setup-surface.tsx` Etapa 4 "Cadastrar instrumento" → `/cadastros`; `ficha-surface.tsx` `goNext` | Scroll position and focus carry over from the page that was left. (a) "Cadastrar instrumento" opens Cadastros at `scrollY` 750, with the bottom of the new instrument's panel on screen ("Fechar") and the focus on `BODY`. "Código" is off screen (`J5-01-cadastrar-instrumento-768.png`). (b) "Próxima ficha", mouse or keyboard Enter, opens PR-ENEL and SEC-ENEL at `scrollY` 593 and 596, mid-checklist, with the header and plate above the fold and the focus on `BODY` (`V-sheet-PR-ENEL-768-light.png`). (c) The Sumário reached by "Concluir dados do relatório" leaves the focus on `BODY`. A keyboard or screen-reader user loses their place on each forward step. A finger has to scroll back up. | **Fix in an Epic 12 follow-up:** reset scroll on route change (sheet to sheet, Cadastros entry), and land focus on the surface heading or first field, as setup already does with its Etapa heading. |
| E12-Q3 | medium | test defect (gate flake) | 12.2 DoD (J0, J5 recorded), gate | `e2e/journey-forward.spec.ts:83-90` (`openRow`), `:189-190` | In `verify`, **12.2-E2E-001** failed ("Continuar: Obra da árvore · 0 de 94" not found; the button read "PR-ENEL · 0 de 94"), and it failed again 1 of 2 when run alone. `openRow` reads `.app-bar-title` after waiting only for it to be non-empty. Since 12.5 the Sumário's App bar title is the relatório name, so the stale title "Obra da árvore" is taken as the sheet TAG. **12.2-E2E-003** clicked "Voltar" right after the URL became `/cadastros`, before `useBackTarget`'s effect set the return target, and landed on the Sumário. It passed 2 of 2 alone. | **Fix now:** wait for the title to equal the row's TAG (`data-block-id` row text), and wait for the panel before pressing Voltar. The `@p0` gate should not flake on its own new tests. |
| E12-Q4 | medium | product question with a data consequence | 12.3 AC4 (D-3), 12.4 AC4 (third reason), AR-20 | `apps/api/src/db/seed.ts:230-243` (`seedStandardTemplate`, idempotent by name) | A company seeded before PR #37 keeps its **v1** "Cabine primária — padrão" template, and every relatório created from it is v1. On such a database "Igual à" still copies IDENTIFICAÇÃO, Nº SÉRIE and TAG (J-02, a P0 of the review), and the dialog offers no "Equipamento inacessível". Only a fresh seed gets v2, which is why this QA stack showed the fix working. Batch C listed this as "Open for Matheus: move v1 templates to v2?". | **Decide before the pilot:** either (a) a one-shot op that moves the unedited standard template to `SEED_VERSION` (relatórios keep theirs), or (b) accept v1 on old databases and say so in `source-deltas.md`. |
| E12-Q5 | medium | AC deviation (narrowing not recorded in the story) | 12.3 DoD "J3 at most 5 taps and about 36 keystrokes"; 12.6 AC1 "J3 at most 5 taps … at most 40 keystrokes" | `e2e/tap-budget.spec.ts:34-36` (`TAP_BUDGET.J3 = { taps: 7, keys: 47 }`) | The browser pass measured J3 at **7 taps / 49 keys** (Enter included), on the first try every time. The 5-tap target assumed a copied serial, which D-3 now forbids, so the two per-unit fields cost 2 taps and about 15 keys. The coordinator decided the threshold, but `epics.md` still reads 5 / 40. | **Matheus:** amend 12.3 DoD and 12.6 AC1 (strike through, dated) to 7 taps / 47 keys with the D-3 reason, or ask for a change that fits 5. |
| E12-Q6 | medium | test gap (known open) | 12.1 AC1 and DoD | `apps/web/src/input/press-hold.ts`; `e2e/lost-taps.durability.spec.ts` | Per the 12.1 triage log, the gate (`desktop-chrome` + `durability-desktop-chrome`, mouse) stays green with `useHeldWhilePressed` removed. Only `durability-android-chrome` in the matrix, which `verify` does not run, catches its loss. The stale-label path ("Próxima ficha" concluding on the fresh rows) has no deterministic test. The mechanism reads correct: the cap is cleared on `pointerup`; release comes on click+0, 300 ms after up, `pointercancel` or 800 ms; handlers write through the queue on fresh rows, so a held render cannot duplicate or drop an edit; keyboard input is never held. | **Next epic:** run one lost-tap spec with CDP touch on a `@p0` Chromium project (the `humanTap` `touch` option exists), so removing the hold fails `verify`. |
| E12-Q7 | low | risk (device) | 12.4 AC2 (focus lands in the combobox) | `registry-picker-field.tsx:46-60` | "Outro…" focuses the input two animation frames after the press, outside the user gesture. In Chrome the combobox had focus and typing worked at once. iPadOS Safari, and some Android WebViews, show no soft keyboard for a focus outside a gesture, so on a tablet the second tap may come back. | Check on the target tablet. If the keyboard does not open, focus synchronously in the press handler and scroll afterwards. |
| E12-Q8 | low | visual vs mock | 12.5 AC4 | `apps/web/src/surfaces/ficha/ficha-header.tsx` (`.sheet-summary` plain text); `components.css:392-393` | The mock draws the missing count in `.n-missing` (amber, 600, tabular) and done steps in `.n-done`. The app renders the sentence as one plain string, so "faltam **9** leituras" loses its emphasis (compare `M-sheet-v09-tablet.png` with `V-sheet-PR-ENEL-768-light.png`). | Kernel returns parts (text + kind), web wraps them. |
| E12-Q9 | low | visual | 12.3 AC2 (`cabine-line`) | `packages/domain/src/relatorio/cabine.ts` `lineMeasure` | At 768 in both themes, a long type ("ALVENARIA - CONVENCIONAL") wraps the line and splits "25" from "°C" onto the next line (`V-sheet-PR-ENEL-768-dark.png`). | Join value and unit with U+00A0 in the kernel text. |
| E12-Q10 | low | copy (authored) | 12.3 AC3, 12.5 AC4 | `sheet-progress.ts` `sheetSummaryText` (placa part) | On the cabine's first sheet (CE-ENEL, no plate fields) the header reads "Faltam **6 campos da placa**", but all six are cabine fields ("Da cabine"). Batch C recorded that the cabine fields count in the Placa step. The sentence names them wrongly. | Bruno or Matheus: e.g. "faltam 6 campos da cabine", or count the cabine separately in the sentence. |
| E12-Q11 | low | UX (taps) | 12.2 AC6 (J5) | `new-relatorio-dialog` / Home create flow; `setup-surface.tsx` Etapa 4 | J5 measured 14 taps against batch B's 13. (a) After "Criar “Condomínio Teste”" the focus does not move to "Local (obra)", which needs a tap. (b) The instrument just registered from Etapa 4 comes back unchecked, which needs a tap before "Concluir dados do relatório". | Move the focus to Local after Cliente is created. Check the instrument that the Etapa 4 detour created. |
| E12-Q12 | low | a11y | 12.2 AC1 | `apps/web/src/surfaces/home/relatorio-card.tsx:26` | `[title, status, card.counter?.text].filter((part) => part !== undefined)` keeps a `null` counter, so a card whose relatório is not on the device is named "⟨título⟩, ⟨status⟩, " (trailing separator). | Filter on `!= null`. |
| E12-Q13 | low | UX | 12.4 AC4 | `apps/web/src/surfaces/relatorio/not-tested-dialog.tsx` | Tapping "Outro" reveals "Descreva o motivo", but the focus stays on the chip, so typing needs another tap. Nothing is preselected, the primary is disabled with "Escolha um motivo", and "Equipamento inacessível" is offered (AC met). | Focus the text field when "Outro" is chosen. |
| E12-Q14 | low | visual | 12.5 AC2 (sheet at 768) | rail collapsed strip (`.ficha` rail at tablet width) | At 768, the collapsed "Árvore do relatório" strip paints its background only about 190 px tall: a white box in light, a black one in dark, with the page colour below it (`J1-01`, `V-sheet-PR-ENEL-768-*`). | Let the strip fill the column height. |

Counts: 1 high, 5 medium, 8 low.

## Per-AC pass/fail

| Story | AC | Result | Evidence |
|---|---|---|---|
| 12.1 | AC1 tap applied once after a commit (50/200/400 ms) | **pass** | `lost-taps.durability.spec.ts` 12.1-E2E-001..006 green on the three matrix projects. In the browser, "Outro…" right after typing, "Repetir" right after Enter in Nº série and the bulk right after a Tab commit each applied on the first press with their toasts. Gate weakness: E12-Q6. |
| 12.1 | AC2 one shared fix, mechanism named, J1/J3 before and after | pass | `input/press-hold.ts` header names React Aria's `usePress` cancel on a moved target. It is used once, in `ficha-surface.tsx:135`. |
| 12.1 | AC3 a section completed by a tap stays open; collapses on leaving; never with an out-of-limit reading | pass | Bulk completion left Verificações expanded (`class="ficha-step"`), and so did the NC chip on TP-ENEL. `stepMayCollapse` is a kernel rule that includes `outOfLimit`. Known open: a section left by a tap never collapses later (by the AC's letter). |
| 12.1 | AC4 the mirror leaves the bar when the checklist is complete | pass | After the bulk, the sticky bar held only the stepper and "Próxima ficha". The list head keeps its disabled action with its reason. |
| 12.1 | DoD durability matrix gains the race test | pass | Matrix EXIT 0 with the new spec. |
| 12.2 | AC1 "Continuar" opens the last sheet; counter from the kernel | pass | J0 = 1 tap to TC-ENEL (the last sheet). The card read "3 de 94 fichas" and its name "…, Em campo, 3 de 94 fichas". Test flake: E12-Q3. |
| 12.2 | AC2 "Concluir dados do relatório" goes to the Sumário with section 9 open, "Dados salvos" | pass | The URL became `/relatorio/:id`, section 9 was `is-open` and the status region held "Dados salvos". Focus: E12-Q2(c). |
| 12.2 | AC3 "Voltar" from a sheet: Sumário with the row focused (tablet), tree on phone | pass | At 768 the focus was on `s9-eq-open` CE-ENEL with section 9 open. At 390 Voltar went to `/arvore`. |
| 12.2 | AC4 "Próxima seção" beside "Voltar ao sumário" | pass | Section 2 sticky bar: "Próxima seção · Voltar ao sumário", focus on the "Seção 2 — Definições" heading. |
| 12.2 | AC5 dates default to today, "Criar relatório" enabled | pass | Both dates 25/09/2026 when the dialog opened. |
| 12.2 | AC6 Etapa 4 "Nenhum instrumento cadastrado" + "Cadastrar instrumento", back after "Fechar" | pass with defects | The band and the button (described "Abre Cadastros › Instrumentos") are there. "Fechar" came back to `setup?etapa=4` with the Etapa 4 heading focused. The entry lands at the bottom of the panel (E12-Q2a) and the new instrument comes back unchecked (E12-Q11b). |
| 12.2 | DoD J0 1 tap, J5 no forced Voltar | pass | J0 1; J5 14 taps, 0 Voltar. |
| 12.3 | AC1 suggested instrument per test kind, written only by "Concluir ficha" | pass | SEC-ENEL-2 showed two amber "Sugerido" headers with the mock's helper. After "Concluir ficha" the outbox held `test/isolacao/instrument`, `test/resistencia_contato/instrument` and `concluded_by` in **one batch**. Nothing was written before that. |
| 12.3 | AC2 one-line cabine block, expanded on the first sheet or while incomplete | pass | CE-ENEL (first) was expanded. PR-ENEL was expanded while fields were empty and showed the `cabine-line` "Cubículo Enel · ALVENARIA - CONVENCIONAL · 13,8 kV · 380 V · 1.500 kVA · 25 °C · 65 % · Editar" once complete. Wrap: E12-Q9. |
| 12.3 | AC3 cabine fields counted on the first sheet, "falta ⟨campo⟩" on the Sumário, preIssue warns | pass | CE-ENEL "Faltam 6 …" (copy: E12-Q10). The Sumário section 9 read "Cubículo Enel: faltam 6 campos · …". The Sumário said "Nada impede gerar" (not blocking). |
| 12.3 | AC4 "Igual à" never copies IDENTIFICAÇÃO, Nº SÉRIE, TAG | pass on seed v2 | After "Igual à SEC-ENEL?": Identificação and Nº série empty, TAG = SEC-ENEL-2, toast "Copiado de SEC-ENEL" with Desfazer. Not on v1 databases: E12-Q4. |
| 12.3 | AC5 TAG prefilled "Do bloco · editável" | pass | The SEC-ENEL TAG field read "SEC-ENEL" with the helper. It was not counted as missing. |
| 12.3 | DoD J3 at most 5 taps / about 36 keys | **fail (narrowed)** | 7 / 49 measured; threshold 7 / 47 (E12-Q5). |
| 12.4 | AC1 plate fields visible, no "Digitar" | pass | All ten SEC-ENEL fields were visible on open, with the chips above them. |
| 12.4 | AC2 "Outro…" focus inside the combobox; "Criar" creates offline | pass with defect | The focus was on `INPUT[role=combobox]` both times. Creating with a unit loses the value: E12-Q1. Device risk: E12-Q7. |
| 12.4 | AC3 sheet observation suggested "Item ⟨n⟩: …", confirmed with the conclusion; no typing for Com restrições | pass | TP-ENEL: NC plus the chip gave "Item 1: acúmulo de poeira e sujeira" as an amber Suggestion with its own Confirmar, and the conclusion suggested "Aprovado · Com restrições?" (`J2-01-NC-suggested-observation-390.png`). |
| 12.4 | AC4 three reasons + "Outro" with required text, nothing preselected | pass | Checked from the sheet menu. The primary was disabled with "Escolha um motivo", and with "Outro" and no text it asked "Descreva o motivo". |
| 12.4 | DoD J2 one chip, 0 characters; pass at 390 and 768 | pass | J2 = NC + chip, 0 keys, checked at 390 and 768. |
| 12.5 | AC1 CSS folded, byte-identical, `.frame-*` translated, DESIGN 0.9.0 | pass | `cmp` identical. The `.frame-phone .sum-row` rules are mirrored in `app.css`. |
| 12.5 | AC2 components match the v0.9 mocks in both themes at 390/768/1024/1280 | pass with lows | Checked at 390, 768 and 1280, light and dark, against `key-equipment-sheet-v09.html` and `key-relatorio-overview-v09.html`. 1024 landscape was not captured, by the batch or here. Differences: E12-Q8, E12-Q9, E12-Q14, and the stepper's double underline (known). |
| 12.5 | AC3 sentence-case labels, acronyms kept | pass | Screen labels "Tensão primária", "Tipo de SE", "Nº série". The DOCX was not opened. Unit tests cover the caps. |
| 12.5 | AC4 one kernel sentence in the header | pass | Examples: "Placa e verificações prontas · faltam 9 leituras e a conclusão", "Falta 1 campo da placa, 12 verificações, 9 leituras e a conclusão", "Ficha completa". |
| 12.5 | AC5 Sumário title = relatório name; v0.9 rows; 390 text ≥ 60 % | pass | Title "Condomínio Teste · Torre Norte". At 390 the text column was 81 % of the row, with no horizontal scroll (`V-sumario-390-light.png` against `M-sumario-v09-phone.png`). |
| 12.6 | AC1 J1 ≤ 12 and J3 ≤ 5 taps, ≤ 40 keys, one constant | **partial** | J1 9 / 36 (test). J3 is held at 7 / 47, not 5 / 40 (E12-Q5). |
| 12.6 | AC2 ERGO-E2E-001 48 x 48 / 56 tall at 390 and 768 | pass | `@p0` green, `ERGO_EXCLUSIONS` empty. |
| 12.6 | AC3 both `@p0`, gate under 15 min | pass | `@p0` e2e ran in 9.8 min. The gate went red twice, on E12-Q3 flakes, not on these tests. |

## Tap counts (tablet 768 px)

| Journey | Review baseline (d149a22) | Batches | Measured here | Notes |
|---|---|---|---|---|
| J0 resume | 2 taps | B: 1 | **1 tap** | Home "Continuar: TC-ENEL · 3 de 94" opened TC-ENEL. |
| J5 nothing to first sheet | 12 taps, 56 keys, 1 Voltar, + about 22 taps to register an instrument | B: 10 with the instrument registered, 13 registering it from setup | **14 taps, 44 keys, 0 Voltar**, instrument registered from Etapa 4 | +1 Local field focus, +1 checking the new instrument (E12-Q11). Cadastros opens scrolled to its end (E12-Q2). |
| J1 SEC-ENEL, plate typed | 24 taps + 1 lost, 87 keys | A: 24; C: 21 / 83 | **21 taps, 72 keys, 0 lost** | Two native selects counted at 2 taps each. Typing "15 kV" in Tensão loses the value, and redoing it costs +3 taps and +2 keys (E12-Q1). |
| J1 copied plate | n/a | D: 9 / 36 | not re-measured (needs a prior visit) | From `TAP_BUDGET.J1`, green in the gate. |
| J3 SEC-ENEL-2 | 9 taps + 2 lost, 36 keys | A: 9; C and D: 7 / 47 | **7 taps, 49 keys, 0 lost** | Igual à, Identificação, Nº série, Repetir, first cell, Confirmar, Concluir. The instruments are the suggested ones, 0 taps. |
| J2 one NC | +2 taps, about 45 keys (observation written twice) | C: one chip, 0 keys | **2 taps (NC, chip), 0 keys** | The sheet observation is suggested and confirmed with the conclusion text. |
| J4 não ensaiado | 3 taps (reason preselected) | n/a | **4 taps** | Menu, item, reason, primary. The extra tap is by decision (nothing preselected). |
| J6 generate DOCX | 3 taps, about 15 s | n/a | **3 taps, about 7 s** | The dialog still reports "Emitido" after revision 1 (J-19, out of scope). |

## Known-open items from the coordinator log

| Item | Status found |
|---|---|
| 12.2 "Continuar" with every sheet done opens the first sheet; with no sheet, the Sumário; counter only on on-device cards | Confirmed by code (`resume.ts`, `cards.ts counterOf`). The on-device-only counter produces E12-Q12. |
| 12.2 "Continuar" pressed before its target loads opens the Sumário | Confirmed by code (`home-surface.tsx` `continueCard`, `resume` live query). Low. Not hit by hand. |
| 12.2 narrowings: panel opened directly, `/arvore` by URL, "Próxima seção" secondary | Confirmed. The "Fechar" return pushes history, so browser Back from setup goes to Cadastros again (acceptable). |
| 12.1 root cause and fix | Mechanism reviewed. It is sound, see E12-Q6. |
| 12.1 a section left by a tap never collapses later | Confirmed. Product question below. |
| 12.1 stale "Próxima ficha" label concluding | Confirmed by code (`ficha-surface.tsx:311`). In that case the suggested instruments are written under a button labelled "Próxima ficha", not "Concluir ficha" (D-4's confirming tap). Low. Listed below. |
| 12.1 gate does not fail without `useHeldWhilePressed` | Open: E12-Q6. |
| C narrowings (last-visit copy keeps per-unit fields; v1 stays v1; cabine counted in Placa; suggested observation required until confirmed; cabine editable on a not-tested sheet) | All confirmed. v1 leads to E12-Q4, "cabine in Placa" to E12-Q10. |
| C prefilled TAG never written; Epic 7 must read `nameplateTagPrefill` | Confirmed: no `nameplate/tag` op in the outbox for SEC-ENEL or SEC-ENEL-2. Tracked in deferred-work. |
| C J3 threshold counts the per-unit fields | E12-Q5, pending the AC amendment. |
| D P- captures without 1024 landscape; contrast table corrected | Confirmed. `contrast.test.ts` is green in unit. 1024 was not captured here either. |
| D "before" mocks now render in v0.9 | Confirmed by design (the promoted `components.css`). |
| D stepper double underline; section 9 header narrow at 390; disabled step named; unused `is-done`; sentence-case rules | Double underline seen at 768 and 1280. The section 9 header at 390 was not re-captured. The rest are deferred as logged. |
| D Sumário chevron opens the row on touch | Covered by 12.5-E2E-001 (green). The chevron is `aria-hidden`, and `.sum-open` stays the keyboard control. Not re-tapped by hand. |

## Open questions (for Matheus or Bruno; not decided here)

1. **Matheus, v1 templates (E12-Q4).** Move the unedited standard template to seed v2 on existing databases, or accept v1 there? Without a move, D-3 and the third Não ensaiado reason do not reach relatórios on a database seeded before PR #37.
2. **Matheus, the J3 threshold (E12-Q5).** Amend 12.3 DoD and 12.6 AC1 to 7 taps / 47 keystrokes, or keep 5 / 40 and ask for a change that reaches it.
3. **Matheus, D-2 residue.** A section left by a tap stays open for the rest of the visit. Should a stepper tap, or "Próxima ficha", fold every complete section?
4. **Matheus, D-4 residue.** Right after the last value, a primary still labelled "Próxima ficha" concludes the sheet and writes the suggested instruments. Is that acceptable as the "confirming tap", or must the label update first?
5. **Bruno, authored copy.** "Ficha completa"; "falta ⟨campo⟩" / "faltam ⟨n⟩ campos"; "N campos da placa" when the fields belong to the cabine (E12-Q10); the pre-issue row text; the "Equipamento inacessível" justification.
6. **Bruno, observation in print.** Whether the printed OBSERVAÇÕES repeats the NC items (review § 5 item 2). It is still open, and 12.4 now writes the item lines into the sheet observation on confirm.

## Re-check (PR #39)

Branch `fix/epic-12-qa`, head `7eb8f75`, on the same `fasor-qa12` stack rebuilt at contract version 3. Browser data was cleared, the users re-seeded and a new relatório created. The full gate was not re-run (PR #39's own `verify` is green). Checked in the browser (Playwright MCP, desktop Chrome, 768 and 390) and by reading the diff.

| Item | Result | Evidence |
|---|---|---|
| E12-Q1 "15 kV" through "Outro…" then "Criar" | **pass** | On SEC-ENEL-2, Tensão de placa "Outro…", typed "15 kV", "Criar “15 kV”", then a blur into Corrente nominal. The input keeps "15 kV", and the outbox holds `voltage_class` create plus `nameplate/tensao_de_placa "15"` with **no `null` put**. Tensão is not among the missing fields. The header reads "Faltam 7 campos da placa", the correct count: ten fields, minus Tensão, Corrente and the prefilled TAG. The fix gives the input the created row's label (`wordLabel`), and `voltageMatchKey` makes "15" and "15 kV" name the same row. |
| E12-Q2 scroll and focus after forward navigation | **pass** | At 768: "Concluir dados" lands at `y=0` with focus on the App bar `H1` (relatório name). Opening a sheet from the Sumário: `H1` "SEC-ENEL", `y=0`. "Próxima ficha" pressed from the conclusion at the page bottom, by mouse and by keyboard Enter: `H1` of the next sheet, `y=0`. Home "Continuar" pressed with Home scrolled 400: `H1` TP-ENEL, `y=0`. "Voltar" from a sheet: the Sumário row `s9-eq-open` TP-ENEL focused and scrolled to it. "Próxima seção" pressed at the page bottom: the "Seção 4 — Requisitos básicos" heading focused, `y=0`. "Cadastrar instrumento": Código `INPUT` focused, `y=0`; "Fechar" returns to Etapa 4 with its heading focused and scrolled to. At 390: "Próxima ficha" `H1` at `y=0`, "Voltar" to `/arvore` with its `H1` focused, and the Cadastrar / Fechar round trip the same as at 768. |
| E12-Q4 upgrade of the unedited standard template | **pass (read)** | `seed.ts` `upgradeStandardTemplate` sends one `system:identity` batch through `applyServerBatch`: puts of `seed_version`, `blocks`, `skeleton` and `version` = 1. It runs in **one transaction under `lockCompany`**. The `before` hook re-reads the row inside the lock and aborts, via `UpgradeNoLongerNeeded` and a rollback, when the row is gone, already at `SEED_VERSION`, or `version` ≠ 1. `template/{id}/seed_version` is a new **server-only** family (`path.ts`), and `CONTRACT_VERSION` and `MIN_CONTRACT_VERSION` are 3. **A failure cannot half-apply**: the batch is rejected whole before the transaction, or rolled back inside it. An error other than "no longer needed" propagates, so the seed fails loudly. **A device edit that lands first** bumps `version` (D-4, `apply.ts:317`) and so blocks the upgrade. See E12-R1 for an edit made offline before the upgrade that lands after it. |
| E12-Q6 does spec 007 fail without `useHeldWhilePressed`? | **pass, not deterministic alone** | Mutation run: `ficha-surface.tsx:100` changed to `const state = live` in the working tree only, then restored with `git checkout`. With the hold removed, specs 001 and 007 each ran twice on `durability-desktop-chrome`: **001 failed 2 of 2** ("bulk at 50 ms") and **007 failed 1 of 2**. The control run with the hold passed 4 of 4. Removing the hold now turns `verify` red, through 001 every time and 007 about half the time. 007 alone is a probabilistic witness, not a deterministic one. |
| E12-Q7 "Outro…" focus inside the gesture | **pass (code, browser)**; device check still open | `revealCombobox` uses `flushSync` and then `input.focus()` inside the chip's press handler, with no `requestAnimationFrame`. In Chrome the focus was on `INPUT[role=combobox]` at once. The soft keyboard on the target tablet cannot be checked in emulation. |
| E12-Q11 focus after "Criar" and the new instrument | **pass** | After "Criar “Condomínio Teste”" the focus is in the Local (obra) input, and "Torre Norte" was typed with no tap. After "Criar “15 kV”" the focus stays in the combobox. The instrument registered from Etapa 4 comes back **checked**, and "Concluir dados do relatório" is enabled at once. |
| E12-Q13 Não ensaiado "Outro" | **pass** | Tapping "Outro" moves the focus to the "Descreva o motivo" input. Typed text enables "Marcar não ensaiado". |

New findings:

| ID | Sev | Kind | Where | Evidence | Disposition |
|---|---|---|---|---|---|
| E12-R1 | low | edge case (sync) | `apps/api/src/db/seed.ts` `upgradeStandardTemplate` | A device that edited the v1 template's `blocks` or `skeleton` offline, before the upgrade, and syncs after it: its content put applies per field on top of the v2 content and bumps `version`. The template then carries v1-derived blocks under `seed_version` v2. v1 and v2 differ only in seed definitions (`per_unit` flags, the third reason), not in template block shapes, so nothing breaks today. A future seed version that changes block shapes would inherit the mix. The upgrade also runs only when the seed CLI runs, not on API start. | Note in the retro. If a later seed version changes template shape, reject a content put whose base predates a `seed_version` change. |
| E12-R2 | low | test | `e2e/lost-taps.durability.spec.ts` 12.1-E2E-007 | Without the hold it failed 1 of 2 (see E12-Q6), so on its own it can pass with the fix removed. 001 carries the gate today. | Optionally add `--repeat-each` or several timings to 007 so it fails every time on its own. |

After the checks the stack was taken down with `docker compose down -v`.
