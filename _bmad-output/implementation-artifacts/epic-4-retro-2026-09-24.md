---
epic: 4
date: 2026-09-24
verdict: accepted-with-open-items
criteria: declared
headless: false
---

# Epic 4 retrospective: Projects, relatório setup, the Sumário and the tree

## Epic summary

- Epic 4 covers stories 4.1 to 4.8. All eight are `done` in `sprint-status.yaml`, and `detect-epic --epic 4` returned `pending_stories: []`.
- Diff range: `91f1664..d2e4d6c`, 10 commits, no merge commits, 418 files. `git_evidence.py` ranks the largest additions as `e2e/relatorio.spec.ts` (+910), `setup-surface.tsx` (+863), `relatorio-tree.test.tsx` (+754), `relatorio-tree.tsx` (+652), `sumario-surface.test.tsx` (+644) and `sumario-surface.tsx` (+641/-218).

  | PR | Stories | Merge commit |
  |---|---|---|
  | #25 | 4.1 + 4.3 (batch A) | d687495 |
  | #24 | 4.8 (batch D) | 71ae39e |
  | #26 | 4.4 + 4.5 (batch B) | 831f49a |
  | #27 | 4.2 + 4.6 + 4.7 (batch C) | 4caf1df |
  | #28 | integrated QA fixes Q1-Q14 (batch F) | 488ab5e |

- Coordinator commits on main: `9603635` (R-009 deferral, D-4 decision, epic context), `e7d273b`, `76aa35e`, `6e66090` (status flips and dated narrowings; fable retired for opus on 2026-09-24), and `d2e4d6c` (QA review and screenshots).
- Evidence used:
  - the specs `spec-4-1-4-3-*`, `spec-4-4-4-5-*`, `spec-4-2-4-6-4-7-*`, `spec-4-8-*` and `spec-epic-4-fix-qa.md`;
  - the batch reviews `reviews/epic-4-{A,B,C,D,F}-review.md`;
  - the integrated QA review `reviews/epic-4-review-qa.md` (Q1-Q15 and the re-check after #28) with the screenshots under `reviews/qa-epic-4*/`;
  - the PR bodies and comments of #24 to #28;
  - the coordinator's process notes for this session (delivery shape, what went well and badly);
  - an aggregate-view and cross-story-boundary pass run by a subagent over `d2e4d6c`, whose key findings the coordinator re-checked against the source (items 12, 17, 19, 20, 22, 24 below).
- Missing evidence and narrowed scope:
  - The session logs of the batch orchestrators were not read. Process lessons come from the coordinator's own run and the artifacts.
  - `bmad-review` was not invoked on the whole 418-file diff. The five batch reviews and the integrated QA already covered the per-story code. The retro review was narrowed to the cross-story seams and the aggregate views, run inline by one subagent. `docx.ts`, `libreoffice.ts`, `block-palette-field.tsx`, `tag-dialogs.tsx` and the render internals of `relatorio-tree.tsx` were not read in depth. No mock-parity or CSS review was done in the retro.

## Findings

Every finding below was traced to its source. The line numbers are from `d2e4d6c`.

### Fixed during the epic (integrated QA, PR #28)

| ID | Finding | Source | Disposition |
|---|---|---|---|
| Q1, Q2 | "Criar relatório" opened the Sumário, not the setup page. The responsável was not prefilled. Both are 4.2 ACs that sat between batches A and C. | `reviews/epic-4-review-qa.md` Q1, Q2 | fixed in #28, re-checked |
| Q3 | The cover's "Informações adicionais" printed the Etapa 2 Escopo. The fix removed the Etapa 2 "Escopo" field, and `{escopo}` now comes from Etapa 1. The fix agent decided this. | QA Q3; `spec-epic-4-fix-qa.md` | fixed in #28; the decision awaits Matheus (open question 1) |
| Q4 | A second relatório of the same obra minted 94 new equipment rows with suffixed TAGs, which contradicts spine AD-24/AD-25 and the glossary. The coordinator decided to reuse equipment by base TAG and type. | QA Q4; `instantiate.ts` | fixed in #28; dated note under Story 4.1 |
| Q5-Q14 | These covered the Home card losing the obra, a red `test:e2e:full`, a slow reorder redraw and seven nits. | QA Q5-Q14 | fixed in #28, re-checked |
| batch reviews | The drizzle jsonb re-parse broke pulls (A F-1). `NODE_ENV` in the shared compose anchor was hidden by a jsdom shim (D R1). Etapa 1 dates were corrupted while typing (C). The renderer read the dead `setup.atividade` field (C). A ~50% Combobox flake (C). | `reviews/epic-4-A-review.md`, `-C-`, `-D-` | fixed before each merge |

### Open: cross-story boundaries

| # | Lens | Finding | Source | Disposition | Upstream lesson |
|---|---|---|---|---|---|
| 17 | edge case | The Q4 reuse reads only the local Dexie. A device that never pulled an Emitido relatório of the obra mints 94 new rows with the same base TAGs, which gives about 90 "TAG duplicada" findings after sync. Emitido and Em revisão are not auto-pulled. | `new-relatorio-dialog.tsx:80` `equipmentRows(db, project.id)`; `pull.ts:75`; `status/table.ts:40` | fix now | A reuse or identity rule gets a two-device test. |
| 18 | spec-to-impl | There are three rules for "edited since the last revision". The server and the Export dialog count every project-scope op, so an equipment added in relatório 2 makes relatório 1 "edited" and gives it a needless revision n+1. The status advance counts only relatório-scoped ops, which is Q15's half. | `apps/api/src/http/generate.ts:140-155`; `apps/web/src/db/generate-store.ts:64-71`; `commit.ts:164-170` | fix now, together with Q15 | One kernel `editedSince` for every caller. |
| 19 | edge case | `issue` makes the relatório Emitido without checking edits made after `snapshot_seq`. Gerar, then "pode fechar", then an edit to setup gives an Emitido relatório whose document lacks the edit. AD-22 has no "edited while generating" row. | `use-generate.ts:205-207` | fix now | The AD-22 table needs that row. |
| 20 | edge case | An `unchanged` answer emits no `issue`. After Emitido, then back to Em revisão, then Gerar, the status stays Em revisão with nothing pending. | `use-generate.ts:272-273` | fix now | none |
| 12 | ownership | The setup page decides `canComplete = status === 'rascunho'` and calls `statusTable('rascunho', …)` with a hard-coded state. That is a status rule in apps/web (AD-1/AD-13). | `setup-surface.tsx:136,140` | fix now | none |
| 21 | edge case | A stale "Desfazer" on the section text overwrites newer typing, and write errors are swallowed. The surface bypasses the undo queue. | `section-text-surface.tsx:118,138-150` | fix now | see 5 |
| 22 | spec-to-impl | The Sumário labels an edited section "texto do template". Story 4.7 never refined the word, although the comment says it would. | `packages/domain/src/relatorio/sumario.ts:124-126` | fix now | A stub comment needs a deferred-work entry with an owner. |
| 23 | cross-story | A section 3 customised in the template hides the setup exclusions in the DOCX, because `ownText` wins. | `instantiate.ts:105`; `print/layout.ts:190-191` | fix now or record | none |
| 24 | edge case | Blank exclusions print as empty bullets, and an exclusion cannot be removed. | `setup-surface.tsx:361-365`; `section-variables.ts:56-64` | fix now | none |
| 25 | spec gap | A Rascunho relatório can generate and keeps Rascunho with revision 1 and no banner. `export.spec.ts:110` pins this as intended, but no note in `epics.md` records it. | `pre-issue.ts:64`; `status/table.ts` | product decision (open question 3) | none |
| Q15 | cross-story | Renaming a shared TAG silently changes another Emitido relatório of the obra. Q4 made this the common case. | `reviews/epic-4-review-qa.md` re-check; `deferred-work.md` | fix now with 18 | none |

### Open: aggregate views

| # | View | Finding | Source | Disposition |
|---|---|---|---|---|
| 1 | size growth | `setup-surface.tsx` (845 lines) holds five Etapa components, its own op envelopes and four private field hooks. | `setup-surface.tsx:110-161,205-750,760-845` | defer (split into etapa files and a field-hook module) |
| 2 | size growth | `Sumario` is one ~340-line function. It holds data reads, row edits, restore routing, the status move, the banner and the hosting of the tree and Gerar. | `sumario-surface.tsx:83-423` | defer (extract the row actions like `tree-actions.ts`) |
| 3, 4 | size growth | `use-generate.ts` is a 317-line hook. `sumario.ts` carries helpers for the Story 4.1 project and dialog. | `use-generate.ts:96-413`; `sumario.ts:311-409` | accept 3 until Epic 7; defer 4 |
| 5 | duplication | The undo-toast and edit-queue machinery exists three times (composer, `relatorio-editor.ts`, templates list), and the section text skips it. | `template-composer.tsx:128-225`; `relatorio-editor.ts:69-190` | fix now as one shared hook (it closes 21) |
| 6 | duplication | There are two focus-restore helpers with different semantics, and each has its own `focusAfterRemoval`. | `use-reorder.ts:86-106`; `relatorio-focus.ts:15-49`; `template-composer.tsx:100-114` | defer |
| 7 | duplication | The `relatorio/status` put is built in four places, and section text hand-writes `putBlockOp`. | `setup-surface.tsx:142-160`; `use-generate.ts:147-167`; `relatorio-ops.ts:52-54`; `commit.ts:178-195`; `section-text-surface.tsx:95-116` | fix now (small) |
| 8, 9 | duplication / ownership | The committed-ref resync appears six times. The rule "end follows start" appears twice, in the web. The obra-label fallback is re-derived in the web and skips the kernel's `projectLabel`. | `setup-surface.tsx:341-346,442-446,760-845`; `new-relatorio-dialog.tsx:69-73`; `project-surface.tsx:102`; `setup-surface.tsx:279` | fix 9 now; defer 8 (the "end follows start" rule moves to the kernel) |
| 14 | ownership | Two kernel "setup gaps" disagree. The Sumário cover row can show nothing missing while "Concluir" refuses. | `checks/pre-issue.ts:58-65`; `relatorio/setup-complete.ts:26-37` | defer to Epic 7's `preIssue` |
| 15 | hygiene | A stale doc comment says the ART field does not exist. | `print/document-control.ts:28-30` | fix now |
| 16 | op families | Clean. The api writes only `generation_job`, `revision` and `file`, and the web writes no server-only family. | `apply.ts:87-107` | none |

### Open: spec-to-implementation divergences with no dated note

| # | AC | As built | Source | Disposition |
|---|---|---|---|---|
| 27 | 4.1 AC3 | The client Combobox inside Relatório setup is not built. Setup shows the client read-only. | `setup-surface.tsx:273-276`; `spec-4-2-4-6-4-7-*` | record a narrowing |
| 28 | 4.2 AC2 | Geolocation is asked on every setup mount while the altitude is empty, not once per relatório. | `setup-surface.tsx:638-652` | record or fix |
| 29 | 4.5 AC4 | "Restaurar" lists tombstones with no cutoff at the revision. | `sumario.ts:262-288` | defer to Epic 7 |
| 30 | 4.6 AC3 | The backward move sits in the Sumário header Overflow, not on the Relatório card's pill, and goes back one step only. | `sumario-surface.tsx:128,334-340`; `revisions.ts` | record a narrowing |
| 31 | 4.7 AC1 | The op is a whole `config` put. "Restaurar texto do template" restores the seed text, not the template's text. | `section-text-surface.tsx`; `spec-4-2-4-6-4-7-*` I/O matrix | record, or fix with 22 |

### Open: test gate

- Every story has at least one `@p0` e2e. 4.1 has 001-003, 4.2 001, 4.3 001, 4.4 001-002, 4.5 001-002, 4.6 001, 4.7 001 and 4.8 001/004 (grep over `e2e/*.spec.ts`).
- Several ACs are covered only at `@p1`/`@p2`: 4.2 AC4, 4.3 AC3, 4.5 AC3 (sync duplicates), 4.6 AC3, 4.7 restore with undo, 4.8 offline and failure.
- Some ACs are covered only in jsdom or the kernel: the altitude prefill with a reading, the expired-instrument line, the template isolation of 4.7 AC2, and unconfirmed suggestions in 4.4 AC2.
- 4.6-E2E-001 seeds a revision instead of generating one. No e2e runs "generate, edit, Em revisão, generate revision 2", which would have caught 19 and 20.
- The touch tests 4.3-E2E-003, 4.5-E2E-004 and 4.7-E2E-003 run only in `test:e2e:matrix`. `verify` and `test:e2e:full` report them as skipped (`durability.spec.ts:398,449,509`).

### Process

| ID | Finding | Source | Lesson |
|---|---|---|---|
| P-1 | Dependency waves took the renderer off the critical path. 4.8 ran in parallel with 4.1 from the Porto Seguro fixture, up to a named checkpoint. Batch A's stubs had owners (`Section9Tree`, the setup stub, section text, `generate-action`), and every owner closed its stub. | spec Design Notes; `deferred-work.md` stub entries marked closed | Keep this: stubs with owners plus a checkpoint for a dependent batch. |
| P-2 | Every cross-batch defect (Q1-Q7) escaped five per-batch reviews and was found only by one integrated QA pass on clean main. Each batch reviewer judged its own spec, and nobody owned the ACs that span batches. | `reviews/epic-4-review-qa.md` | Run an integrated pass before each epic closes. In the spec phase, name an owner for every AC that spans batches. |
| P-3 | A semantic conflict surfaced only in the full `verify` after merging main into batch B: 4.8 made `SyncState.generate` required, which broke 4.4's mocks. | coordinator commit on the 4.4/4.5 branch, "Add the generate mock…" | Re-verify every branch after main moves. Keep one test factory for `SyncState`. |
| P-4 | Batch C's orchestrator was handed back mid-run while its dev child was still listed as running. The first watcher misfired, because this box's `find` is bfs and rejects relative `-newermt`. | coordinator notes | Add an orchestrator watchdog; use ISO timestamps. |
| P-5 | Three stacks plus concurrent verifies drove the load average to 12-15. A 3.4-E2E-005 timeout, a composer blur test that failed three times and an Alt+Arrow race followed. | the PR #24 and #26 reports | Run at most two concurrent verifies (E3-A4). |
| P-6 | Two decisions were taken without Matheus: Q4 by the coordinator (grounded in the spine) and Q3 by the fix agent. Scope narrowings were recorded in `epics.md` after each merge, not before the work. | `spec-epic-4-fix-qa.md`; commits `e7d273b`, `76aa35e` | A fix agent reports product-level choices as open questions. |
| P-7 | Token cost was high: each batch orchestrator used about 450-600k tokens, and the QA agent used about 820k in two rounds. | coordinator notes | none |

## Behavior verification

The integrated QA agent exercised Epic 4 end to end on an isolated stack (`fasor-qa4`) at `6e66090`, and again at `488ab5e` after the fixes. It used the Playwright MCP browser at 390, 768 and 1280 px, in light and dark, offline, keyboard-only and in two browser contexts:
- Home > client (created inline) > project > "Novo relatório", giving 94 blocks with their TAGs;
- every setup Etapa, and Rascunho > Em campo;
- the Sumário: order, meta, header counts, reorder and undo, and restore;
- section text, and that the template stayed untouched;
- the tree and the block operations;
- Gerar relatório up to revision 2, with the DOCX opened and checked: cover, document control, TOC pages and section texts;
- the Emitido > Em revisão banner and the backward move;
- a second relatório reusing the 94 equipment rows.

After #28:
- `pnpm verify` was green: unit 709 + 718 + 20, api 139, e2e `@p0` 49.
- `test:e2e:full` was green: 90 passed and 3 touch tests were skipped (they pass on Android).
- Screenshots are in `reviews/qa-epic-4/` (R-*.png for the re-check).

The retro itself ran no browser. Items 17-24 come from reading the code and were checked against it, but they were not reproduced at runtime.

## Previous-retro follow-through

Transitions proposed from the evidence; statuses are written only after Matheus confirms:

- `epic-1-retro-item-1-a1-run-the-manual-ipad-and-android-offli`: no evidence found (`test-artifacts/manual/` has no new run). Proposed: none. It still blocks Epics 5 and 6.
- `epic-1-retro-item-12-a12-before-any-exposure-beyond-the-offic`: not applicable yet. Proposed: none.
- `epic-2-retro-item-16-e2-a4-dod-every-user-facing-ac-exercised`: partly landed. Every Epic 4 story has a `@p0` and the narrowings carry dates, but ACs remain covered only in jsdom (see the test gate). Proposed: `in-progress`.
- `epic-2-retro-item-17-e2-a5-human-style-pass-covers-each-form`: landed. Every batch pass and the QA pass covered 390 px, and the QA pass ran a second browser context. Proposed: `done`.
- `epic-3-retro-item-18-e3-a1-record-the-r-009-seed-review-under`: deferred by Matheus on 2026-09-23 to before Epic 5 (`epics.md` Story 3.1 note). Proposed: none.
- `epic-3-retro-item-19-e3-a2-decide-the-not_tested_reason-for-t`: no evidence found. Proposed: none.
- `epic-3-retro-item-20-e3-a3-validate-sheet-path-segments-again`: not landed. `packages/domain/src/ops/path.ts:19-20` still calls it open. Epic 5 writes sheet values. Proposed: none; it must land before Epic 5.
- `epic-3-retro-item-21-e3-a4-stabilize-the-gate-templates-list`: not landed, and P-5 shows it got worse under parallel load. Proposed: none.
- `epic-3-retro-item-22-e3-a5-decide-d-4-template-version-d-5-co`: D-4 was decided and built (PR #25). D-5 and D-7 are still open. Proposed: stay `in-progress`.
- `epic-3-retro-item-23-e3-a6-process-a-seed-fixture-freeze-requ`: no new freeze happened in Epic 4. Proposed: none.
- `epic-3-retro-item-24-e3-a7-process-one-story-per-pr-or-a-reco`: landed. Each batched spec carries `warnings: ['batched']` with a reason, and the Dev model change is struck through with a date in `epics.md` (`e7d273b`). Proposed: `done`.
- `epic-3-retro-item-25-e3-a8-process-focus-asserted-after-every`: landed. There are 32 `toBeFocused` assertions in `e2e/relatorio.spec.ts`, `tree.spec.ts` and `export.spec.ts`, and the touch ACs 4.3-E2E-003, 4.5-E2E-004 and 4.7-E2E-003 ran on `durability-android-chrome`. Proposed: `done`.
- `epic-3-retro-item-26-e3-a9-deferred-polish-toggle-sub-lines-f`: no evidence found. Proposed: none.
- `epic-3-retro-item-27-e3-a10-tooling-remove-stale-worktrees-re`: partly landed. The emoji is gone from the PR bodies (#24-#28 checked), but `git worktree list` shows 20 entries. Proposed: `in-progress`.

## Action items

All items are proposals. Code items go through the normal dev loop, and spec reconciliations wait for Matheus.

1. **E4-A1 (fix now, before Epic 5 reads relatório status)**: Make the generate and status seams correct:
   - one kernel `editedSince`, restricted to the equipment the relatório references (18 and Q15);
   - no `issue` over edits made after `snapshot_seq` (19);
   - `issue` on `unchanged` (20);
   - the setup page asks `statusTable(status, 'setup_complete')` (12).

   Add a `@p0` e2e that runs "generate, edit, Em revisão, generate revision 2". Owner: dev (opus).
2. **E4-A2 (fix now)**: Q4 across devices. Pull the project's equipment before "Criar relatório", or refuse to create offline without it (17), and add a two-device test. Owner: dev.
3. **E4-A3 (fix now)**: Section text and exclusions:
   - one shared undo-toast/edit-queue hook, which closes the stale "Desfazer" and the swallowed write errors (5, 21);
   - the Sumário meta says the text was edited (22);
   - blank exclusions are filtered in the kernel, and an exclusion can be removed (24);
   - decide whether a template-customised section 3 or the setup exclusions wins (23).

   Owner: dev.
4. **E4-A4 (small fixes)**: One `putRelatorioStatusOp` and `putBlockOp` for every status and config write (7). The web uses the kernel's `projectLabel` (9). Drop the stale ART comment in `document-control.ts` (15). Owner: dev.
5. **E4-A5 (spec reconciliation)**: Record dated narrowings in `epics.md` for 27 (no client Combobox in setup), 28 (geolocation asked per mount), 30 (backward move in the header, one step) and 31 (a `config` put; restore to the seed text). Decide 25 (may a Rascunho relatório generate?). Schedule 29 and 14 with Epic 7. Owner: Matheus with the coordinator.
6. **E4-A6 (human gate, 4.8 AC)**: Bruno reads `reviews/qa-epic-4-D/porto-seguro-skeleton-rev1.docx`, and his remarks are recorded under Story 4.8. Matheus confirms or reverses the Q3 and Q4 decisions. Owner: Matheus.
7. **E4-A7 (defer, refactor)**: Split `setup-surface.tsx` into etapa files plus a field-hook module (1, 8). Extract the Sumário row actions (2). Unify the two focus-restore helpers (6). Move the project helpers out of `sumario.ts` (4). Owner: dev, in the story that next touches each file.
8. **E4-A8 (process)**:
   - Every epic closes only after an integrated human-style QA pass on clean main (P-2).
   - In the spec phase of a batched epic, every AC that spans batches gets a named owner for its end-to-end wiring.
   - Any identity or reuse rule gets a two-device test.
   - A fix agent reports product-level choices as open questions instead of deciding them (P-6).

   Owner: coordinator / SM.
9. **E4-A9 (process, gate)**:
   - Run `test:e2e:matrix` at each epic close, since the touch tests skip in `verify` and `full`.
   - Run at most two concurrent verifies on this machine.
   - Add a shared `SyncState` test factory.
   - Add an orchestrator watchdog that checks the worktree for quiet with ISO timestamps (P-3, P-4, P-5).

   Owner: dev / coordinator.

## Acceptance verdict

**Accepted with open items**, against the declared criteria (the Epic 4 ACs in `epics.md`, read together with their dated narrowing notes).

- Every story is `done`, and the automated and hands-on evidence on main at `488ab5e` shows the ACs met. The integrated QA re-check lists Q1-Q14 fixed and no regression.
- Open items:
  - The Story 4.8 human AC (Bruno's reading) is pending.
  - Items 17-24 and Q15 are real defects at the seams. None of them corrupts a delivered document in the tested single-device flow, so none blocks acceptance. 17, 18 and 19 must land before Epic 5 builds on relatório status and equipment identity (E4-A1, E4-A2).
- By process lesson (d) of `bmad-build.toml`, `epic-4` stays `in-progress` in `sprint-status.yaml` until the Bruno AC has evidence.

## Open questions

1. Q3: is removing the Etapa 2 "Escopo" field, with `{escopo}` now coming from Etapa 1 "Informações adicionais", the right reading of the cover? Or should seed v2 give the cover and section 1 separate variables, together with D-2?
2. Q4: is reuse by base TAG and type the right identity rule for a second visit? Should it later follow renamed TAGs, as recorded in `deferred-work.md`?
3. Item 25: may a Rascunho relatório generate a revision, or should Gerar require Em campo?
4. Item 23: when a template customises section 3, which one prints, the template text or the setup exclusions?
