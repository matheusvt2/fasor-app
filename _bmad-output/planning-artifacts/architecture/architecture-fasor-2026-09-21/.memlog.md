---
scope: Releng MVP (codename fasor): web-only, tablet-first, offline-first field tool that generates FO.SERV-03 as DOCX/PDF; whole system at feature altitude
purpose: build-substrate
altitude: feature
updated: 2026-09-21T14:53
---

- (event) run started 2026-09-21; inputs: PRD (final, 75 FRs, two-week slice 7.3 due 2026-10-03), PRD addendum (architecture handoffs, seed spec), UX EXPERIENCE.md v0.8.0 + DESIGN.md v0.8.0, UX review-architecture.md; greenfield, no code yet, no spec package
- (constraint) Inherited from PRD/UX (not re-decided): responsive web app only, no PWA install (2026-09-21); one device fills a relatorio at a time in MVP, multi-device merge deferred; all AI in backend, offline queue; PDF and DOCX both rendered from app data, never DOCX->PDF conversion; generated conclusion text is a device-side template, not AI; criteria are sourced data; UI system TBD by architecture; DESIGN.md tokens must map without changing values; must run on Android Chrome, iPadOS Safari, desktop Chrome/Edge; pt-BR, America/Sao_Paulo assumed
- (decision by user) Fast path chosen: Winston drafts the full spine with [ASSUMPTION] tags; Matheus corrects in review
- (decision by user) Spine is the only deliverable; no extra human-facing artifact needed
- (version) Verified on the web 2026-09-21: Node 24 Active LTS; React 19.3.0; Vite 8.3 (Rolldown); TypeScript 7.0.2 (Go-native; 6.0 is the last JS-based); React Router 8.4; react-aria-components 1.21.1; Dexie 4.4.6; Zod 4.6.5; Hono 4.13.8; Drizzle ORM 0.45.2 (1.0 still beta); PostgreSQL 18.6; pg-boss 12.33; better-auth 1.7.5; docx 9.7.1; LibreOffice Still 26.2.6 / Fresh 26.8; sharp 0.35.4; Google Cloud Vision DOCUMENT_TEXT_DETECTION USD 1.50 per 1000 after 1000 free/month; Claude models: claude-opus-5 USD 5/25 per MTok, claude-sonnet-5 USD 2/10, claude-haiku-4-5 USD 1/5
- (constraint) Safari/WebKit eviction verified: all script-writable storage of an origin is deleted after 7 days of Safari USE without user interaction on that origin (not 7 calendar days); persist() is refused on iOS Safari; home-screen install (ruled out by PRD) would exempt it. A 3-day job with daily use never trips it; the exposure is a tablet with unsynced data left idle for a week
- (decision) AD paradigm: local-first client with an operation outbox + thin sync API; modular-monolith server; shared pure domain kernel (packages/domain) that runs identically on device and server. Binds all. Prevents two implementations of merge rules, pre-issue checks, conclusion templates and grouping (one function, two renderings)
- (decision) AD unit of change = sub-block-addressed operation {op_id uuidv7, entity, entity_id, path, value, actor, device_id, ts}; client outbox in IndexedDB; server applies idempotently by op_id and materializes state; pull by per-relatorio cursor. MVP merge policy = last-writer-wins per path (single device); the deferred multi-device merge is a policy swap inside packages/domain over the same ops. Prevents whole-document blob saves that make sub-block merge a rewrite
- (decision) AD ids: UUIDv7 client-generated for every entity, photo and op; server never mints ids for client-created rows; retries dedupe by id. Prevents offline creation waiting on the server
- (decision) AD ownership map: Company (tenant) owns registries and users; Project = client + site container (Site collapsed into Project [ASSUMPTION], resolves PRD Q4); Equipment (TAG) owned by Project, unique (project_id, tag), referenced by Equipment blocks -> rename keeps identity, cross-visit copy reads by equipment_id; Relatorio owns block tree, locations, sheet data, photos, points, revisions and carries a SNAPSHOT of the template block definitions at creation (template_id + template_version for provenance) -> FR-13 holds by construction; Cabine (root Location) owns SE characteristics, environment, agrupar_por_tipo
- (decision) AD location = tree node (parent_id nullable): cabine is a root node, column a child; equipment attaches to any node; depth not fixed in code, UI renders two levels. Resolves PRD Q13 (Enel: equipment directly under the cabine; 1o Subsolo: PTMT/IM panels) without a third named concept
- (decision) AD photo durability: original Blob stored in IndexedDB at capture with its own uuid; uploaded by idempotent PUT /photos/{id} carrying sha256; deleted locally only after server ACK; reading-queued photos first; client-side thumbnail (max 512px) for gallery; server keeps originals in S3-compatible object storage keyed company/{cid}/relatorio/{rid}/photo/{pid}; originals retained after generation (purge deferred)
- (decision) AD offline vehicle: service worker with precached app shell (assets only, no manifest install prompt, no Background Sync API) so a cold open of the browser tab works offline [ASSUMPTION: asset caching is not 'installation' under the web-only decision]; sync engine runs while the tab is open: on launch, on online event, on interval; no reliance on persist(); unsynced data older than 5 days [ASSUMPTION] raises a warning banner; the ops+photos outbox is the only thing that must survive eviction, and it is flushed at every first connectivity
- (decision) AD auth/session: email+password via better-auth, httpOnly cookie session, 30-day sliding [ASSUMPTION]; session continues offline; a 401 during sync keeps the outbox and shows re-auth; local IndexedDB database name is namespaced by user id so two users on one device never share a store; users provisioned by a CLI seed script (no signup)
- (decision) AD tenant scoping: every table carries company_id; the API resolves company from the session and every repository call takes it as a required argument; no RLS in MVP (deferred). Prevents cross-tenant reads when SaaS arrives
- (decision) AD measured values: canonical shape {raw: decimal string with '.', unit, state: measured|not_measured|empty}; pt-BR parsing (comma decimal, point thousands, unit suffix) happens only at the input boundary in packages/domain/parse and echoes back; floats never stored; criteria comparison in the kernel. Derived cells (VAL CALCULADO) are computed, never stored as typed
- (decision) AD suggestion = its own entity, never a field value: {id, relatorio_id, target path, value, trust: suggested|verify, source: {photo_id, bbox, ocr_token_ids}, status: pending|confirmed|discarded}; confirming emits a normal op plus status change; typing into the target discards that suggestion only; pre-issue and progress ignore pending suggestions. Grounding rule (FR-37): a numeric value is 'suggested' only when its digits are covered by the OCR tokens the LLM cited, otherwise 'verify' -- enforced server-side in the reading job, not trusted from the model
- (decision) AD reading pipeline: photo upload with a reading kind (plate|display|caption|panel|nc_obs) enqueues a pg-boss job; job = OCR with word boxes (Google Cloud Vision DOCUMENT_TEXT_DETECTION [ASSUMPTION]) + Claude structuring via the official @anthropic-ai/sdk citing OCR token ids, model claude-opus-5 [ASSUMPTION; claude-sonnet-5 is the cost fallback], structured output via output_config.format; result rows = Suggestions pulled with the relatorio sync; every call logs tokens and USD per reading (unit economics). Estimated cost per job at Opus 5: ~94 plates ~USD 3, ~1100 display shots ~USD 33, ~82 captions ~USD 2.5; Vision OCR ~USD 2
- (decision) AD generation: server-side pg-boss job; input = frozen JSON snapshot of the relatorio + registries taken at job start; ONE renderer: DOCX built with the docx library from a layout spec; PDF = LibreOffice headless conversion of that generated DOCX in the same job (never of an edited DOCX); TOC = static, two-pass (render, read heading page numbers from the PDF outline, write the TOC table, re-render) so DOCX and PDF carry the same TOC; PAGE/NUMPAGES fields for 'Pagina X de Y'; revision number allocated in the same transaction that stores both files, so a failed job consumes none; preview = same job with RASCUNHO watermark, no revision row; one test asserts draft == issued except watermark and revision line
- (decision) AD attribution: sheets store created_by, last_modified_by, concluded_by; the document prints concluded_by falling back to last_modified_by [ASSUMPTION, PRD Q3]
- (decision) AD registry references: Instruments and Clients referenced by id; Manufacturer and Voltage class stored BY VALUE on the sheet with the registry acting as a suggestion list, server dedupes registry rows by normalized name (casefold, trim, strip accents). Resolves the Schneider/SCHNEIDER handoff without client-side reference rewriting
- (decision) AD soft delete: blocks, photos and points carry removed_at (tombstone); recovery until export = clear tombstone; purge policy deferred; delete-vs-edit becomes a conflict only when multi-device arrives
- (decision) AD time: UTC ISO 8601 on the wire and in DB; capture stamp keeps captured_at + tz_offset; display and document in America/Sao_Paulo; photo sort key = (captured_at, device_id, local_seq), numbers assigned at generation from the snapshot and frozen with the revision [ADOPTED from UX]
- (decision) AD dependency direction: packages/domain depends on nothing app-specific (zod only); apps/web and apps/api depend on domain; web talks to api only through the sync/API contract typed in packages/domain/contract; no server-only module is importable from web (enforced by package boundaries + eslint no-restricted-imports)
- (decision) AD UI system: the UX mockups' tokens.css (1:1 with DESIGN.md) and components.css are promoted into apps/web as the styling layer; React Aria Components supplies the accessible headless behavior (dialogs, radiogroups, comboboxes, focus management, stylus/touch press); no Tailwind, no shadcn, no MUI. Prevents token values being re-encoded into a framework theme
- (decision) AD seed data: FO.SERV-03 block type definitions, checklists, table grammars, criteria and section boilerplate live as typed TS in packages/domain/seed with a seed_version; the DB is seeded per company from it and the seeded Template 'Cabine primaria - padrao' records the seed_version; block definitions keyed by (report_type, block_type) with report_type = 'cabine_primaria' as the single value (SaaS/multi-type slot without UI)
- (decision) AD status machine: relatorio status transitions are one table in packages/domain/status; every transition is an op; the table content (PRD Q2) is deferred to the PM but its location is fixed
- (decision) Seed/stack: pnpm monorepo; apps/web (React 19.3, Vite 8.3, React Router 8.4, Dexie 4.4 + dexie-react-hooks, react-aria-components 1.21); apps/api (Node 24, Hono 4.13 + @hono/node-server, Drizzle 0.45, pg-boss 12, better-auth 1.7, docx 9.7, sharp 0.35, @anthropic-ai/sdk, @google-cloud/vision); packages/domain (zod 4.6); PostgreSQL 18; LibreOffice 26.2 Still inside the api Docker image; S3-compatible object storage (Cloudflare R2 [ASSUMPTION]); one Docker image deployed on Fly.io [ASSUMPTION] running api + pg-boss worker in-process (WORKER=1 flag allows a separate instance later); Vitest + Playwright
- (direction) Deferred: multi-device merge policy content and Conflict view (FR-58/59/60, PRD 7.3 wait 3); RLS; purge/retention numbers; dictation engine; geolocation-to-altitude; storage-low threshold; photo compression numbers; export time tolerance; LGPD basis; status table content (Q2); salvar-como-template carry list (Q5); rich text editor (FR-12); PWA install (ruled out); NR-10 text by date
- (event) Reviewer gate run 1: reconcile-prd, reconcile-ux, rubric, versions, adversary written to reviews/; spine rewritten to 26 ADs: typed OpPath + applyOp + seq/LWW + prev_op_id (AD-3), file entity with kinds and variants (AD-7), sync order and rejection semantics (AD-24), Equipment on the relatorio cursor + last_nameplate projection (AD-25), section-8 tokens (AD-26), RelatorioSnapshot + generate barrier + LibreOffice in slice (AD-15), BlockConfig + append-only seed (AD-21), provisional status table (AD-22), same-origin serving (AD-9), Source deltas section
- (version) Corrections from review-versions: PostgreSQL 17 (Fly MPG offers 16/17 only); TypeScript 6.0 (typescript-eslint supports <6.1, crashes on 7.0.2); R2 has no bucket versioning -> immutable keys + scheduled bucket copy; @hono/node-server 2.1.1; drizzle-kit 0.31.11; pnpm 12.5; LibreOffice 26.2 via TDF .deb (Debian trixie apt = 25.2.3), 26.2 EOL 2026-11-30; @anthropic-ai/sdk 0.127.0; @google-cloud/vision 6.0.0; Vitest 5.0.1; Playwright 1.63.0; Safari 7-day rule unchanged through Safari 26.x/27.0 and also wipes SW registrations
- (decision) Vocabulary: spine uses relatorio per the user's 2026-09-19 decision; PRD glossary 'Laudo' is stale -> Source deltas. Section-9 scheme: kernel/contract carry scheme per relatorio (AD-16), Export surface does not expose it per UX S6 decision; PRD FR-68/FR-72 wording stale -> Source deltas. EXPERIENCE.md install surfaces stale per PRD 2026-09-21 -> Source deltas
- (constraint) Web research 2026-09-21 on OCR/LLM language: frontier VLMs (incl. Claude Opus) hallucinate coordinates (CER > 0.5 on grounded-OCR benchmarks) -> the LLM must never emit boxes; grounding must come from a real OCR layer (validates AD-12/AD-14 split). Seven-segment displays (FR-36 'Ler visor'): best results are PaddleOCR DBNet detection + PARSeq recognition on the 7Seg dataset, glare is the failure mode; these are Python-native (PaddleOCR 3.x on PaddlePaddle 3.0+), with ONNX exports and a JS/ONNX port (@paddleocr/paddleocr-js 0.4.2) of lower maturity. Anthropic Python and TS SDKs have parity for vision + structured outputs. Google Cloud Vision is strong on printed text (nameplates), unproven on seven-segment
- (decision by user) Backend language (2026-09-21): TypeScript monolith stays (shared kernel is the load-bearing invariant; Anthropic and Vision SDKs first-class in TS). OCR step becomes an OcrProvider interface owned by the reading job: Google Cloud Vision in the slice (nameplates, printed text); a Python sidecar service (FastAPI + PaddleOCR PP-OCRv5 detection + PARSeq recognition + OpenCV glare/deskew preprocessing) added when FR-36 'Ler visor' ships, because seven-segment displays are where cloud OCR fails and the Python ecosystem is the only mature one. Alternatives weighed: whole backend in Python (kernel duplicated or offline rules lost) and TS-only cloud OCR (FR-36 risk concentrated on the cloud). LLM never emits coordinates (VLMs hallucinate boxes); grounding always from the OCR layer
- (version) OCR sidecar versions verified 2026-09-21: Python 3.14.7 current but PaddleOCR 3.5.0 supports <=3.13 -> sidecar on Python 3.13; uv 0.12; FastAPI 0.141.1; PaddleOCR 3.5.0 (2026-04-21); PaddlePaddle 3.4.0; opencv-python 4.14.0; onnxruntime 1.25; @paddleocr/paddleocr-js 0.4.2 exists as a JS/ONNX port (lower maturity, not chosen)
- (event) Reviewer gate run 2 (rubric-2, adversary-2): 22/23 and 10/10 first-pass findings closed; new findings applied in a full rewrite: one log for everything (server-only op families emitted as system:* actors: file uploaded_at/variants/reading_status, suggestion create, equipment last_nameplate, generation_job, revision, relatorio/preview_file_id), pull = {ops, seq, summary}; project/relatorio create families with kernel instantiateTemplate; file as a discriminated union on kind with photo variant, owners point at files, file/{id} create before bytes; scope company|project|relatorio with equipment in project scope and streams defined as queries; provenance materialized as sheet cell source_suggestion_id by applyOp, device-side compareSuggestion; field kinds and value shapes (AD-11); OcrProvider contract (print variant, boxes in input pixel space, reading_run row); sheetState precedence and derived attribution (AD-18); role only in BlockConfig; editedSince(snapshot_seq) as the single predicate; coalescing boundary, dead ops excluded from state, 426 only on pull; src/api third fetch site; libs pinned
- (constraint) Correction of the inherited constraint recorded at entry 2: the PRD says the PDF is never a conversion of an EDITED DOCX (FR-62); converting the job's own generated DOCX inside the generate job satisfies it (AD-15). The earlier wording 'never DOCX->PDF conversion' was too broad
- (version) Support libraries verified 2026-09-21: typescript-eslint 8.70.0; fractional-indexing 4.0.0; exifr 7.1.3; postgres (postgres.js) 3.4.9; @aws-sdk/client-s3 3.1134.0; uuid v7 support since uuid@10 (pin 11+); heic-to (libheif 1.22) for gallery HEIC imports
- (decision) Source deltas recorded in the spine for PRD/UX to absorb: Laudo->relatorio; section-9 scheme not exposed; FR-22 counts vs decoded document; FR-34 scope = equipment.last_nameplate; Nao ensaiado reasons without Acesso; UX install surfaces removed; thumbnails pulled, originals on demand; 'Igual a' = same type and manufacturer; dictation hidden in the slice
- (event) spine finalized (26 ADs, 16 [ASSUMPTION] tags, 2 reviewer-gate runs, lint clean except intentional path placeholders)
- (decision by user) Containers everywhere (2026-09-21): local development always runs in Docker (docker-compose: web, api, postgres, minio, ocr sidecar when it exists); the SaaS deploys on a managed container service, never a VM with host-installed runtimes; the same image is built once and promoted local -> staging -> production. Fly.io Machines stays the seed provider [ASSUMPTION]; Cloud Run, ECS/Fargate and Railway are the alternatives weighed. Promoted from seed to AD-27 because LibreOffice, fonts and Node in the image are exactly where local and cloud drift
- (event) spine updated: AD-27 added (containers everywhere), Stack runtime row and Environments paragraph amended; status stays final
- (decision by user) Cloud strategy (2026-09-21): cloud-native, every service inside one cloud; AWS is the target, another provider only when AWS lacks the service. Consequences verified on the web: Claude runs on Amazon Bedrock (Opus 5 available since 2026-07-24, structured outputs GA on Bedrock, ZDR by default) via @anthropic-ai/bedrock-sdk Mantle client, first-party API kept as a config fallback; OCR in the slice = Amazon Textract DetectDocumentText (WORD blocks with bounding boxes, USD 1.50 per 1000 pages, replaces Google Cloud Vision); Postgres = RDS for PostgreSQL 18 (Aurora/RDS support 18.3 since 2026-06, so PG 18 returns); object storage = S3 native with bucket versioning (replaces R2 and the scheduled-copy workaround); containers = ECS on Fargate behind an ALB with images in ECR; secrets = Secrets Manager; logs = CloudWatch. Fly.io, R2 and Google Vision dropped
- (event) spine updated for AWS: AD-27 rewritten (cloud-native, one cloud, AWS services listed, sa-east-1 and CDK as assumptions), AD-14 providers = Textract + Claude on Bedrock with first-party fallback, Stack/Seed/diagram/Environments amended; PostgreSQL back to 18; versions verified: @anthropic-ai/bedrock-sdk 0.33.7, aws-cdk-lib 2.269.0, @aws-sdk 3.1134
- (decision by user) No LLM service yet for the POC (2026-09-21). Verified in official docs (code.claude.com legal-and-compliance; support article 15036540): subscription OAuth is 'intended exclusively' for Claude Code and native Anthropic apps; developers building products 'should use API key authentication through Claude Console or a supported cloud provider' and may not 'route requests through Free, Pro, or Max plan credentials on behalf of their users'; Agent SDK and claude -p in the developer's OWN projects currently draw from the subscription's usage limits (credit program paused 2026-06-15). Consequence: the backend never uses the personal subscription; LLM_PROVIDER/OCR_PROVIDER env with a 'fake' fixture provider as the docker-compose default (zero cost for dev and tests), 'anthropic' with a Console pay-as-you-go key for the POC (~USD 3 per job for 94 plates at Opus 5), 'bedrock' once the AWS account exists. Personal claude -p / Agent SDK remain fine for the developer's own experiments (prompt tuning on a photo folder, seed transcription), not as an app backend
