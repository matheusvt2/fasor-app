# Material bruto de cliente (fotos, áudios, vídeos, laudo original) - repo é público
docs/context/
docs/media/

# Screenshots e logs de sessões do Playwright
.playwright-mcp/

# Config pessoal do BMAD (regenerada pelo installer)
_bmad/config.user.toml
_bmad/custom/config.user.toml

# Claude Code local
.claude/settings.local.json

# Sistema / editores
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# Dependências e builds (para quando o app existir)
node_modules/
dist/
build/
.env
.env.*
!.env.example
# Locally-generated mkcert CA and leaf certificates (Story 1.7)
certs/

.bmad-loop/runs/
.bmad-loop/cache/
.bmad-loop/policy.toml
_bmad/render/
playwright-report/
test-results/

# Scratch folder of the "migrations match schema.ts" test (drizzle-kit needs a relative --out)
apps/api/.drizzle-check/
